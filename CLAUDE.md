# CLAUDE.md — Repo Navigation Map

Purpose: let a Claude session locate code without reading whole files. Paths are relative to this file. `ikigai_masala-main/` is the only project.

> Keep this file in sync when you add/rename/move modules. If you change a path or symbol referenced below, update the corresponding line.

---

## 1. Top-level layout

```
ikigai_masala-main/          active project (Indian menu planner)
├── app.py                   Streamlit frontend; auto-spawns Flask backend
├── api/                     Flask REST backend
├── src/                     solver + rules + preprocessor + client/history/db
│   ├── settings.py          shared leaf: settings the data layer needs (no api/ import)
│   ├── ontology/            per-city item lists: paths.py (resolution) + repository.py (loading + caches)
│   └── application/         orchestration that is neither HTTP nor pure domain
├── ui/                      Streamlit-side API client & formatters
├── customisation/           Streamlit editor UIs for per-client config
├── data/                    per-city menu ontologies (raw/city_items/*.xlsx) + rule configs (configs/city_rules/*.json)
│   └── raw/source_workbooks/  every client-supplied workbook, one place — the provenance of each derived rule (see its README)
├── scripts/                 Supabase seeders + SQL schema
├── tests/                   pytest suite
├── pytest.ini, requirements.txt
├── README.md                overview + quick start
└── docs/                    setup.md, architecture.md, api.md, operations.md, data_fixes_for_client.md, client_logics.md, pune_rulebook.md, pune_client_logic.md, chennai_client_logic.md, **client_rules_index.md** (GENERATED — every client's rules in one place, `scripts/dump_client_rules_index.py`), **pending_config_changes.md** (the DB values + scripts an operator still has to apply) (the two .xlsx that lived here moved to data/raw/source_workbooks/)
```

---

## 2. Entry points

| Command | File | What it does |
|---|---|---|
| `streamlit run app.py` | `ikigai_masala-main/app.py` | UI; auto-starts Flask on port 5000 |
| `flask --app api.app run` | `ikigai_masala-main/api/app.py` | API standalone |
| `pytest` | `ikigai_masala-main/pytest.ini` | run tests |

Secrets: `SUPABASE_URL`, `SUPABASE_KEY` in env or `.streamlit/secrets.toml`.

---

## 3. API surface (all in `ikigai_masala-main/api/app.py`)

| Method | Route | Purpose |
|---|---|---|
| GET  | `/api/v1/clients` | list clients (`clients` = names; `clients_detail` = `[{name, city, is_launch_site}]` for the sidebar city + launch-site filters) |
| POST | `/api/v1/plan` | generate full menu (a 500 from an INFEASIBLE solve now carries the pre-flight `rule_diagnostics` + `summary` — it cleared the blocking gate, so its *warnings* are the only clue to which rules conflict; `ui.api_client.SolverFailedError` (a `RuntimeError` subclass, same shape as `RuleDiagnosticsBlockedError`) surfaces them) (optional `counter_index` picks which counter to solve; optional `alternates` 0..`MAX_ALTERNATES`=4 → ranked near-optimal distinct menus in an `alternates` list, primary stays in `solution`; response carries `counter_mode`/`counter_count`/`counter_index`/`counter_name`; each item carries `is_nonveg`) (optional **`shared_items`** = `[[iso_date, slot_id, item], …]` → cross-counter common-category sync: `_merge_shared_items` folds them into `forced_items` (same narrow-the-cell mechanism as a client constant), an explicit constant pin wins; the planner passes the primary counter's shared-slot dishes so later counters serve the same dish per day — see note 22) |
| POST | `/api/v1/regenerate` | regenerate selected cells (optional `counter_index`; items carry `is_nonveg`; optional **`exclude_items`** = `[[iso_date, {slot_id: [item_base, …]}], …]` → the dishes already shown for each cell this session, folded into the regenerator's `extra_forbidden` so repeated clicks stop cycling A→B→A) |
| POST | `/api/v1/save` | persist plan → history (single `week_plan`, or multi `counters: [{name, week_plan}]` → nested `menu_history`). **Dish names are canonicalised to the ontology's spelling first** (`_canonicalise_saved_plan`, note 28) — history is read back by the cooldown/freshness/cadence rules, which compare the stored string against `pool['item']`, so a display name stored as `dal tadka` would silently never match `dal_tadka`. What cannot be resolved is stored as given and reported in `unknown_items` + `warning` (both absent on a clean save); **never rejected** — an off-ontology `constant_items` pin is legitimate and a generated plan must not be lost to an unrecognised name |
| GET  | `/api/v1/editor-metadata` | slot/theme/**city** metadata for editor (`available_cities`, `default_off_slots`, `default_item_cooldown_days`, `available_client_pools`, `client_pools_by_city`). Optional `?city=` scopes `available_client_pools` to that city's item list; without it the list is the cross-city union. The editor does **not** use that union — `customisation.main.pools_for_city` reads `client_pools_by_city` and returns `[]` for a city with no pools, because offering a Bangalore token to a Pune client is worse than offering none (the PUT rejects it, and it would match nothing in Pune's list anyway) |
| POST | `/api/v1/pool-preview` | F5: eligible distinct item count + category-wise counts for a set of `source_pools` (common always included). Optional `city` selects which city's item list is counted |
| GET  | `/api/v1/client-config/<name>` | fetch client config (incl. `city`, `serve_weekends`, `item_cooldown_days`, `source_pools`, `is_launch_site`, **`shared_categories`** — base slots synced across counters: the DB `clients.shared_categories` value when set, else the file value from `MenuRuleLoader.get_shared_categories`) |
| PUT  | `/api/v1/client-config/<name>` | update client config (accepts `city`, `serve_weekends`, `item_cooldown_days`, `source_pools`, `shared_categories`) |
| POST | `/api/v1/client` | create client (accepts `city`, `serve_weekends`, `item_cooldown_days`, `source_pools`, `shared_categories`) |
| DELETE | `/api/v1/client/<name>` | delete client |
| POST | `/api/v1/diagnose` | dry-run pre-flight: run every rule's `diagnose()` + built-in pool/colour diagnostics; returns structured `rule_diagnostics` + `summary` + `counter_index`/`counter_name`/`counter_count` (the same pass `/plan` runs as its 422 gate). **Honours `counter_index`** — it used to build its inputs from the primary counter regardless, so every multi-counter client got a clean bill of health for counter 0 while the counter being planned was unsatisfiable |
| GET  | `/api/v1/saved-plan` | fetch a previously saved plan from history |
| GET  | `/api/v1/metrics` | Prometheus-style metrics scrape |
| GET  | `/api/v1/health` | health check |

Helpers:
- `api/concurrency.py` — `@solver_gate` queue; caps active solves (dynamic CP-SAT workers).
- `api/config.py` — path constants, day/time limits, **per-city ontology resolution** (`city_excel_path`, `city_required_slots`, `CITY_ITEMS_DIR`, `DEFAULT_ONTOLOGY_CITY`).
- `api/rate_limit.py` — per-IP token-bucket throttle on `/plan` + `/regenerate`.

Hardening (all opt-in / non-breaking):
- `MAX_CONTENT_LENGTH_BYTES` (default 2 MB) caps request bodies; an over-limit
  body is rejected in `before_request` so the status is 413 rather than a 500
  swallowed by an endpoint's broad `except Exception`.
- `API_WRITE_TOKEN` (env, unset by default) gates the mutating endpoints via
  `@require_write_token` — `X-API-Key` or `Authorization: Bearer`. Unset means
  the API behaves exactly as before (open), so this changes nothing until an
  operator opts in. Reads are never gated.
- Rate-limit buckets: `plan` **`MAX_COUNTERS * 3`/min (18)**, `regenerate` 20/min, `write` 30/min
  (save / create / delete / config PUT), `diagnose` 20/min (also `/pool-preview`).

Beyond the optional write token there is no per-user authentication: reads are
public and the deployment relies on the network perimeter.

---

## 4. `src/` module map

### 4.1 `src/solver/` — CP-SAT solver
| File | Key symbols | Role |
|---|---|---|
| `menu_solver.py` | `MenuSolver.solve`, `SolverConfig`, `_Cell`, `_freshness_bonus` | cell-based CP-SAT: one bool var per (day, slot, candidate). Optional `recency_by_item` ({item_base: days-since-last-served}) drives the soft **freshness** objective (`_freshness_bonus`, note 24) — prefer dishes served long ago / never, `min(days, FRESHNESS_CAP_DAYS=30) * FRESHNESS_UNIT=3000` per candidate, bounded below one LOW soft-rule unit so any rule outranks it; never bans, never INFEASIBLE |
| `solver_context.py` | `SolverContext` | runtime bundle passed to rules |
| `solution_formatter.py` | `SolutionFormatter.to_dict` | solver output → API JSON (tags each item `is_nonveg` from a passed nonveg name-set) |
| `regenerator.py` | `MenuRegenerator.regenerate`, `similarity_score` | locks fixed cells, re-solves selected ones. `regenerate(..., extra_forbidden={(date, slot_id): {item_base, …}})` also blocks every dish already shown for a cell this session, so repeated clicks cycle through fresh dishes instead of flipping A→B→A (soft: falls back to the penalty path if blocking empties a cell). Passes `recency_by_item` through so replacements prefer fresher dishes too |
| `_helpers.py` | `weekday_type_for_config`, `strip_color_suffix`, `items_from_day`, `cell_is_skipped` | shared utilities. `cell_is_skipped(skip_cells, date, slot_id)` is the single skip predicate for `MenuSolver._build_cells` + `MenuRegenerator`: `skip_cells` holds `(date, base_slot)` entries (skip every expansion — what `slot_day_restriction` emits) **and** `(date, slot_id)` entries (skip one expansion — how a client constant pins `nonveg_main__2` while `__1` stays solved) |

### 4.2 `src/menu_rules/` — constraint system
Two-phase: `pre_filter_pool()` (cheap removals before CP-SAT vars), `apply()` (hard/soft constraints on model), `get_objective_terms()` (penalties/bonuses).

| File | Kind | Role |
|---|---|---|
| `base_menu_rule.py` | abstract | parent class |
| `menu_rule_loader.py` | loader | deserialize a rules JSON; `load_for_city(city)` resolves `data/configs/city_rules/<city>.json` + its `extends` chain (override by rule `name`, `disable` list), falling back to `DEFAULT_CITY`; `load_for_client()` merges per-client `{disable, rules, constant_items}` over the city list by name (legacy list form still works) |
| `cuisine_menu_rule.py` | hard | min cuisine variety |
| `color_rules.py` | hard + params | `ColorPairingMenuRule` (two named slots may not share a colour on a day) · `WelcomeDrinkColorMenuRule` (no repeat welcome-drink colour on adjacent days) · **`ColorVarietyMenuRule`** — adds no constraints; it carries the *per-city numbers* for the built-in colour block (`min_distinct_per_day`(`_chinese`/`_biryani`), `max_same_color_per_day`, `max_same_color_reach`, `max_colors_at_reach`, `ignore_rice_gravy_color_diff_on_chinese_day`), read by `api._build_solver_config` via `solver_overrides()` against the `_RULE_OVERRIDABLE_CFG_FIELDS` allow-list. Bangalore wants 4 distinct with one colour allowed to reach 3 (the SolverConfig defaults, so it ships no such rule); Pune's rulebook R1/R2 want 3 distinct and a flat cap of 2, which is why the numbers had to leave `SolverConfig` |
| `unique_items_menu_rule.py` | hard | no repeats within horizon |
| `coupling_menu_rule.py` | hard | deep-fried rice/bread/veg-dry family (rulebook 34-42): liquid rice ⇒ deep-fried veg_dry/starter (scoped to active slots); deep-fried veg_dry ⇒ rice-bread + liquid rice; rice-bread ⇒ liquid rice; dosa bread + active nonveg ⇒ SI chicken gravy (availability-guarded); rice-bread & deep-fried veg_dry each ≤1 day/wk. All conditional upper bounds (never mandates, never INFEASIBLE). The old rice-bread⇔deep-fried-starter link is retired (rulebook §5) |
| `curd_side_menu_rule.py` | hard | fill curd/raita slot (biryani/pulao→raita, else→curd); the `curd_side` slot is displayed as "Curd / Raita" |
| `premium_menu_rule.py` | hard | broad premium min/max/day cap — **retired from the default ruleset** (rulebook §5/45-46); class kept for configs that still want it. Default now uses two `selector_frequency` exact-1 rules: `premium_veg_gravy_exactly_one` (`is_premium_gravy`@veg_gravy) + `premium_veg_dry_exactly_one` (`is_premium_veg_dry`@veg_dry) |
| `welcome_drink_color_menu_rule.py` | hard | welcome drink color |
| `welcome_drink_buttermilk_rule.py` | hard | buttermilk (`is_buttermilk`) on exactly N (default 2) welcome-drink days, solver-chosen, non-consecutive |
| `theme_day_menu_rule.py` | soft | prefer theme-matched items per day |
| `theme_slot_filter_rule.py` | pre-filter | drop non-theme items on theme-heavy days (chinese/**continental**/biryani/south/north); config `exempt_slots` unioned with `EXEMPT_FROM_CUISINE`. The **bread cuisine lock** (south bread on south days, non-south otherwise) now honours `exempt_slots`; it used to run ahead of the exemption check in all three paths — `pre_filter_pool`, `diagnose` (`base in exempt_slots and base != 'bread'`) and `_project_filter_size` — so listing `bread` was silently ignored. Chennai lists it: its bread pool narrows to the 10 south-tagged rows (the dosai/idly family) on a south day, while the client actually serves chapati. Bangalore/Pune don't list it, so they are unchanged. **Cuisine exclusivity**: chinese/continental dishes appear ONLY on their own theme day, and only for cuisine-main slots (`_CUISINE_MAIN_SLOTS` = rice/veg_gravy/veg_dry/starter/nonveg_main); universal slots keep incidental tags. On a continental day the continental veg is the **gravy** — `veg_dry` is never continental (stays a normal Indian dish), so a continental day = continental rice/starter/nonveg/gravy + one Indian veg_dry. **`indian_veg_dry_themes`** (config, default empty) generalises that to any theme: a listed theme's `veg_dry` is narrowed to `north_indian ∪ south_indian` — both kept, so the objective picks whichever suits the day rather than the config pinning a region — and that theme's cuisine is dropped from `veg_dry` even on its own day. Tekion/Stryker/Stripe list `chinese`, so their Chinese Tuesday is chinese rice/gravy/starter/nonveg + one Indian veg dry. `exempt_slots` could NOT express this: `_filter_chinese` never consulted it (exemption worked only implicitly, via a slot being absent from `_CHINESE_FLAG_MAP`, while its `veg_dry` branch ran unconditionally — the same silent-ignore as the bread lock), and a blanket exemption would also switch off south/north/biryani narrowing. Wired in `pre_filter_pool` **and** `_project_filter_size`, or the pre-flight would report a phantom "0 items match the chinese filter" for a slot the solver no longer filters that way |
| `theme_fallback_penalty_rule.py` | soft | penalty when theme cannot be met |
| `theme_starter_preference_rule.py` | soft | bonus for theme-matching starters |
| `item_cooldown_menu_rule.py` | pre-filter | ban recently used items |
| `week_signature_cooldown_menu_rule.py` | soft | avoid recent week signatures |
| `ricebread_gap_menu_rule.py` | pre-filter | enforce N-day gap |
| `nonveg_biryani_weekly_rule.py` | pre-filter | ≤1 nonveg biryani/week |
| `nonveg_dry_preference_rule.py` | pre-filter | prefer dry nonveg on the 2nd nonveg slot — **retired from the default ruleset** (superseded by the `slot_composition` 2-nonveg pair, which it conflicted with on chinese/biryani days); class kept for configs that still want the simple slot-2 dry heuristic |
| `ingredient_ban_rule.py` | pre-filter | per-client banned ingredients (case-insensitive exact match on `key_ingredient` **and** `primary_protein` — e.g. a mushroom ban catches both fields) |
| `item_frequency_rule.py` | CP-SAT | per-client weekly frequency cap via selector (flag/sub_category/item/key_ingredient/primary_protein) |
| `selector_history_window_rule.py` | pre-filter (history) | **generic cross-week cadence**: a selector may recur only once per rolling `window_days` that is LONGER than the plan horizon ("fish once/15 days", "kadhi once/15 days", "oil-based bread monthly"). A single 5-day solve can't see prior weeks, so this reads saved history: `HistoryManager.selector_banned_by_date` finds the selector's last service and, for each planned date inside the window, bans **every** matching item — folded into the same `banned_by_date` the item-cooldown pre-filter already applies (no new solver machinery). `apply()` is a no-op; `matching_items(df)` resolves the selector against the city's ontology in `api.app`, and `_effective_history_window` counts `window_days` so the history query reaches back far enough. Pairs with a within-plan `selector_frequency max`/`daily_max` (this handles across-plans, that handles within-plan). Selector-level twin of the per-dish `item_cooldown`. Wired in the city rulesets (Bangalore special_sambar 15d; Pune kadhi/leafy 15d, oil-bread 30d) and per-client (Stryker fish/biryani/sambar 15d, Siemens kofta 14d) |
| `selector_frequency_rule.py` | CP-SAT | **generic Phase-1 rule type**: selector-driven horizon count (max/min/exact days, day-level) + **`max_per_week`** (per ISO calendar week — what a rule NAMED `*_weekly` has to mean, see note 26; `max` counts across the whole horizon and so silently tightens as the plan grows, `max: 1` becoming "once a fortnight" on 14 days. Both may be set: `max_per_week` for the cadence, `max` for a plan-wide ceiling; a `max_per_week` looser than `max` is rejected rather than silently taking the tighter one, and it cannot be combined with `exact`) + `non_consecutive` + `daily_max` (per-day occurrence cap) + **`daily_min`** (per-day floor — the only way to say *every day* independent of horizon length, since `min` counts days across the horizon: `min: 5` is "5 of 5" on a week but "5 of 10" on a fortnight. Capped per day to what that day can place, so a thin day relaxes instead of going INFEASIBLE) + **`base_slot` may be a LIST** (a constraint that spans slots — "a protein source somewhere on the plate" is satisfied by the rice, gravy, veg dry, salad *or* dal, and scoping it to any one of them would be a different rule; a string still yields the single-slot behaviour every existing config relies on) + **`allowed_day_types`** (forbid the selector outright on days of other themes; skipped when the ban would empty a cell). `allowed_day_types` is what keeps a themed dish on its themed day — a `mix` day is not narrowed by the theme filter at all, so a counter could serve biryani on Monday and none on its actual biryani day. plus **`forbidden_weekdays`** (a weekday-scoped ban, the twin of `allowed_day_types` for a rule that is about the calendar rather than a cuisine — TCL's "no baby corn, paneer or mushroom on Saturday and Sunday", where the weekend days carry no theme and `slot_day_restriction` would stand the whole slot down; same degrade-rather-than-fail path, and it stands alone as a constraint). Selector = flag/sub_category/item/key_ingredient/primary_protein/course_type/cuisine_family, plus `any_flag: [...]`, **`any_of: [selector, …]`** (the general "any of these", needed when one rule names several ingredients across both flag and text columns), **`all_of: [selector, …]`** (the conjunction: "an egg GRAVY" is `is_egg_dish` AND `is_nonveg_gravy`, and neither column alone says it — ICON Chn's Monday egg gravy came back as a boiled egg under the flag alone) and **`name_contains: [substring, …]`** (matches the dish NAME — the escape hatch for a family the ontology's columns file unreliably; `key_ingredient == 'baby_corn'` tags 67 Bangalore rows of which 3 are baby-corn dishes, it being the de-facto default for a mixed salad, while the 34 dishes actually named after baby corn carry `corn`/`bell_pepper`/`cauliflower`. Prefer a column when one is trustworthy). min/exact auto-capped to placeable (availability + non-consecutive aware) so it never forces INFEASIBLE. Config-driven in the per-city rulesets (`data/configs/city_rules/<city>.json`) |
| `attribute_grouping_rule.py` | CP-SAT | **generic Phase-3 rule type**: group a slot's candidates by an attribute (`group_by` = any column, e.g. item_color/key_ingredient) and constrain each distinct value: `non_consecutive` (same value not on adjacent days — rulebook 79 dal-colour) and/or `max_per_group` (each value ≤N days/horizon — rulebook 82 sambar key-ingredient, an empty-history stand-in for the 15-day rolling window). Caps only; never INFEASIBLE on its own |
| `soft_preference_rule.py` | CP-SAT soft | **generic Phase-4 soft rule type** (objective-only, never INFEASIBLE): `mode` = **`prefer_daily`** (penalise every day the selector is ABSENT — the soft twin of `selector_frequency.daily_min`: use the hard floor for the days that must carry it and this for "and the rest too, if the rules allow". Tekion/Stryker want a non-dal protein on their 3 ordinary days hard and on the chinese/biryani days soft, because a hard every-day floor there would trade a whole plan for one dish. Only days that COULD carry it are scored, so a candidate-less day is not a constant the solver cannot act on), `different_day` (penalise two selectors same day — premiums apart, #3), `avoid_consecutive` (penalise selector on adjacent days — regional nonveg/rice, #14-17), `avoid_attribute_repeat` (penalise a slot repeating an attribute value — key-ingredient variety, #1), `prefer_day_types` (penalise the selector on days outside `day_types` — paneer belongs on mix/south/north, with chinese/biryani as the fallback; the hard equivalent `selector_frequency.allowed_day_types` would forbid paneer outright on a counter themed chinese every weekday), **`match_attribute`** (two named slots should agree on one attribute — Citrix's "if veg gravy is north then veg dry should be north" and "flavoured rice and veg gravy same region". `group_by` + a required `values` list + `base_slot_a`/`base_slot_b`, with optional `selector_a`/`selector_b` narrowing. Penalises "slot A serves value v and slot B serves nothing of value v", NOT "the two values differ": a slot may run several cells, and a 2-dish `veg_dry` mandated one north + one south by `veg_dry_north_south_pair` DOES match a north gravy — penalising every crossing pair would fight that composition for no gain. Symmetric, so a single mismatch scores twice; a day where one side carries none of the listed values is skipped entirely, or a chinese day would push the gravy off north AND south. Soft because the client said "if I give south I can use north fallback". `values` is required and bounds the comparison: without it a chinese rice beside a north dal would read as a violation, which is the theme rules' business). `priority` (high/medium/low) selects a weight from `constants.OBJECTIVE_TIER_WEIGHTS` (theme 1e15 > high 1e12 > medium 1e9 > low 1e6, each ~1000× the next) so soft rules apply lexicographically — a higher-priority rule is never sacrificed for a pile of lower ones (rulebook §7). `weight` overrides. `base_slot` accepts a name or a LIST (same as `selector_frequency`), and every mode honours it — a list used to fall through to "all slots". Hard rules stay guaranteed (they're constraints); random tie-break (~1e3/cell) sits below LOW |
| `slot_composition_rule.py` | CP-SAT | **generic Phase-3 rule type**: composes a slot family *per day*, switchable on the day's theme. `base_slot` + a self-gate on the counter's configured slot count: **`min_slot_count`/`max_slot_count` (preferred, a range)** or legacy `requires_slot_count` (exact). The exact form silently excluded every counter serving *more* than the stated number — a 3-dish nonveg counter got no composition at all, so its biryani-theme days came back with no biryani while non-biryani days got two; the shipped ruleset uses the range form and a test pins that. The range also keeps two compositions off the same slot family: `nonveg_main_daily_pair` is `2-4` and `nonveg_main_five_dish` is `5+`, so a 5-dish station (biryani + gravy + dry + kebab + egg daily) composes without the pair also demanding cells. A test asserts at most one nonveg composition is active per slot count. `days_forced_by_composition()` exports what a composition mandates so frequency caps can detect the contradiction. `components` (default) + `components_by_theme` (per-theme override), each a `{selector, count}` list using the `selector_frequency` grammar; per day it adds `sum(matching vars) >= count` (≥, so two `count:1` components across two cells = exactly one of each, but a missing component drops instead of forcing INFEASIBLE). `count` also capped to per-day availability (auto-relax). Base ruleset uses it for the 2-nonveg pair (biryani day → biryani+gravy, chinese day → chinese+gravy, else dry+gravy) and the 2-veg_dry pair (north+south). Needs the theme-filter union (note 16) so the paired regional gravy survives on chinese/biryani days |
| `slot_day_restriction_rule.py` | skip-cells | per-client: skip a slot on certain weekdays (e.g. no nonveg_main on Tue/Thu). Optional **`slot_indices`** skips only the named expansions instead of the whole family — the way to serve *fewer* of a slot on some days rather than none (TCL runs two flavoured rices Mon-Fri and one on Saturday, so only `rice__2` stands down). No solver change: `_helpers.cell_is_skipped` already reads `(date, slot_id)` entries, the same shape a client constant uses to pin `nonveg_main__2` |
| `same_day_exclusion_rule.py` | CP-SAT | **two families of dish may not share a day** (or, with `"scope": "week"`, a whole plan — "biryani not the same week as fish", Stryker NCR): `selector` + `exclude` (+ optional `base_slot`/`exclude_base_slot` to narrow either side; `scope` = `day` default / `week`). Counted across every slot by default, because the point is the day's plate — a paneer gravy beside a soya veg dry is what it forbids. HARD; the one relaxation is arithmetic, a day where BOTH sides are *forced* (some slot has no candidate outside `exclude` AND some slot none outside `selector`) is skipped rather than turning a reportable impossibility into a bare INFEASIBLE. `soft_preference`'s `different_day` is the soft cousin and can be outbid, which is why "don't serve them together" needed a constraint. **A dish satisfying BOTH sides belongs to the selector** (`_hits` drops it from the exclude side): `chole_paneer` is `key_ingredient: paneer` AND `is_chana_gravy`, and counting it twice makes `a + b <= 1` read `1 + 1 <= 1` — the dish becomes unservable on every counter, silently. `diagnose()` and `_forced()` apply the same rule so the report, the relaxation and the solve agree |
| `repeatable_items_rule.py` | declarative | **some dishes in a slot are staples, not variety**: `base_slot` + `selector` (+ `exclude`) marks matching items exempt from `unique_items` **and** from the item-cooldown history ban. Permits repetition; never forces it (that's `fixed_daily_item`). Same `repeatable_item_flags()` hook, so `MenuSolver._declared_repeatable()` → `context['extra_repeatable']` feeds both consumers from one declaration. **`scope`** picks which exemptions apply: `both` (default, as above) or **`cooldown`** — the history ban stands down but `unique_items` holds, so the slot serves distinct dishes within a plan and repeats only once every distinct one is used. That is the `COOLDOWN_EXEMPT_SLOTS` treatment (note 11) made available per client, and it is what a *family* daily needs rather than a single dish: Citrix's welcome drink is buttermilk every day and Bangalore carries 10 buttermilks — fewer than a daily slot needs across a 20-day window, so the ban has to go, but under `both` the solver poured the same tempered buttermilk on Monday and Thursday while eight others went unused. It rides a **separate** hook (`cooldown_exempt_item_flags()` → `context['extra_cooldown_exempt']`) rather than a flag on the first, so a consumer that has not been taught about `scope` keeps the narrower behaviour — forgetting to read it starves a slot visibly instead of silently permitting a repeat nobody asked for. Exists because `REPEATABLE_ITEM_FLAGS_BY_SLOT` is global and staple-ness is regional: Pune's rulebook R36 says plain chapati/phulka may run on consecutive days (its bread pool is those two dishes, so without the exemption the 20-day cooldown empties the slot in week 2), while a Bangalore bread slot rotating naans and parathas wants the no-repeat rule kept |
| `fixed_daily_item_rule.py` | CP-SAT | **one slot's dish is the SAME every day**: `base_slot` + `selector` (+ optional `exclude`). Encoded as one bool per matching candidate plus `sum(that item's vars on day d) == z_i`, so an item is used on every day or none; an item unavailable on some day is excluded rather than made INFEASIBLE. Also exposes `repeatable_item_flags()`, which `MenuSolver._declared_repeatable()` collects into `context['extra_repeatable']` and `unique_items` folds into its repeatable set — so the rule creating a repetition and the rule forbidding one cannot disagree, and the exemption stays scoped to the client that asked for it (unlike the ontology-wide `REPEATABLE_ITEM_FLAGS_BY_SLOT`). L&T's non-veg station uses it for the daily egg; its kebab is already fixed by having one eligible item |

### 4.3 `src/preprocessor/` — data pipeline
Flow: `ExcelReader.read` → `ColumnMapper.apply` → `DataCleanser.clean` → `PoolBuilder.build_pools`. Theme filtering is **not** here — it lives in the rules layer (`src/menu_rules/theme_rules.py::ThemeSlotFilterRule`) so it can run per (date, slot) with the day's theme.

| File | Key symbols |
|---|---|
| `excel_reader.py` | `ExcelReader.read` |
| `column_mapper.py` | `ColumnMapper.apply` (alias detection, derived `key_eff`) |
| `data_cleanser.py` | `DataCleanser.clean` |
| `pool_builder.py` | `PoolBuilder.build_pools(df, required_slots=None)`, `_base_slot`, `_expand_slots_in_order` (expands `veg_dry` → `veg_dry__1, veg_dry__2`), `_nonveg_mask` (build_pools drops non-veg items from every slot except `nonveg_main`; it likewise drops `is_curd_rice` dishes from the plain `rice` pool, because the curd-rice station is flag-driven while the dish's `course_type` is `rice`, so every curd rice sat in BOTH pools and a counter running both slots could serve the same dish twice on one day — ToastTab CHN did. `unique_items` cannot stop it: the curd-rice staple declaration exempts the dish so it may recur across days, and that exemption also permitted the same-day pair). `required_slots` = base slots that must come out non-empty; `None` means every mandatory one (right for a whole-product ontology), a **city** ontology passes its declared set from `city_items/ontology_categories.json`, `set()` skips the check |
| `client_pool_filter.py` | `parse_client_pools`, `get_active_pools`, `item_is_eligible`, `filter_eligible`, `available_pool_tokens` — F5 client-based item-pool eligibility (pure) |

### 4.4 `src/client/` — client config (Supabase, live reads)
- `client_config.py` → `ClientConfig` (dataclass, incl. `serve_weekends`), `ClientConfigLoader`, plus counter helpers `default_counter`, `normalize_counter`, `MAX_COUNTERS`. Per-category frequency is bounded by `_MIN_SLOT_COUNT`/`_MAX_SLOT_COUNT` (1..**5**); `customisation/multi_slot_editor.py` imports both so the editor can never offer a value the loader would clamp. The ceiling was 3, which made a real counter unconfigurable — a non-veg station serving biryani + gravy + dry + kebab + egg needs 5 `nonveg_main` slots and the editor answered "outside the allowed range". City: `AVAILABLE_CITIES` + `normalize_city`; a client's `city` is a plain `clients.city` column (not per-counter). `serve_weekends` is a plain `clients.serve_weekends` bool (Sat/Sun coverage). `item_cooldown_days` (`clients.item_cooldown_days`, nullable; None = `DEFAULT_ITEM_COOLDOWN_DAYS`=20) overrides the item-cooldown window per client. `is_launch_site` is a plain `clients.is_launch_site` bool (feature F, launch view): every existing client is false (the column defaults false), and a client created through the launch view is true — read/written via `get_client_is_launch_site`/`set_client_is_launch_site`, carried in `get_client_row`/`list_clients_with_city`, and an `is_launch_site` arg on `create_client`. `shared_categories` is a nullable `clients.shared_categories` JSONB (base slots synced across counters — the editor's toggle+multiselect; see note 22), read/written via `get_client_shared_categories`/`set_client_shared_categories` + a `create_client` arg, normalised by `_normalize_shared_categories_value` to known base/const slots. All read/written via `get_client_city`/`set_client_city`/`get_client_serve_weekends`/`set_client_serve_weekends`/`get_client_item_cooldown_days`/`set_client_item_cooldown_days`/`create_client(...)`. `AVAILABLE_THEMES` includes `continental` + the weekly-alternating `chinese_continental`.
- No in-memory cache; every read hits Supabase. Reads are **consolidated**: `get_client_row(name)` selects every config column in one query and `api.app._client_row()` memoises it on Flask's `g` for the request, so `GET /client-config` costs 1 round trip (was 7) and `/plan` 2 (was 6). `get_client_configs_from_row(name, row)` builds the per-counter `ClientConfig` list from an already-read row. Writes are **atomic**: `update_client_atomic(name, expected_version, fields)` applies every field plus the version bump in ONE conditional `UPDATE` (`WHERE name=? AND version=?`), replacing a bump-then-N-setters sequence in which a validation failure part-way left the row half-written with the version already incremented. `normalize_counters_for_write()` / `primary_counter_patch()` are the write-free validation halves used to check the whole payload before anything is persisted. `create_client()` is validate-then-insert for the same reason: it takes `working_days` and `source_pools` directly so they are part of the one INSERT — they used to be applied by follow-up setters *after* the row existed, so a rejected pool token answered 400 while leaving a real client behind with no pools (and the retry then failed on the duplicate name). There is no standalone version-bump helper: bumping `version` separately from the fields it guards is the ordering that bug came from, so `update_client_atomic` is the only write path.
- `ClientConfig.counter_name` records which counter a config came from, so per-counter rule overrides can be scoped. Supabase tables (consolidated to 4): `clients`, `app_settings`, `menu_history`, `week_signatures`.
- **F5 client item pools**: `clients.source_pools` (JSONB, nullable) stores the ontology `client`-column pool tokens this client draws from; `common` is implicit. `get_client_source_pools` returns `None` (column missing → callers use the full ontology), `[]` (unset → common-only), or the token list. `set_client_source_pools` normalizes/dedupes and strips `common`.
- Default day themes: Mon=mix, Tue=chinese, Wed=biryani, Thu=south, Fri=north.
- **Client config is one JSON document.** `clients.counters` (JSONB) is the single source of truth — an ordered, non-empty list `[{name, categories, slot_counts, theme_map}, …]`. `counters[0]` is the **primary** counter that `MenuSolver` plans from (`get_client` derives `ClientConfig` from it); extra entries are additional cuisine stations. `get_client_configs` yields one `ClientConfig` per counter — the API solves each independently (client-orchestrated: the planner calls `/plan` once per counter with `counter_index`), and the planner renders one table per counter (tabs) with per-counter regenerate/clear + a shared save/download. Mode is *derived*: `single` ⇔ 1 counter, `multi` ⇔ 2+. `get_counters_for_client` / `get_counter_setup` / `set_counters_for_client` / `update_primary_counter` read/write it. The old normalized `menu_categories` / `slot_count_overrides` / `theme_overrides` tables were folded into this column (premature normalization — config was always read/written per-client, never cross-client). The loader keeps a guarded `_legacy_primary_counter` fallback for a database that hasn't run `scripts/setup_all.sql` yet.

### 4.5 `src/history/`
- `history_manager.py` → `HistoryManager.banned_items_by_date`, `.ricebread_ban_by_date`, `.recent_week_signatures`, `.explode_history_rows`.
- Tables: `menu_history` — **one JSONB row per (client, service_date)**, `menu = {slot: item_base}` (PK on `(client_name, service_date)`); cooldown readers `explode_history_rows()` it into per-item rows in memory. `week_signatures` — weekly hash for week-level cooldowns.

### 4.5b `src/ontology/` — per-city item lists (read side)
| File | Key symbols | Role |
|---|---|---|
| `paths.py` | `city_excel_path`, `city_required_slots`, `city_slug`, `CITY_ITEMS_DIR`, `DEFAULT_ONTOLOGY_CITY`, `DEFAULT_EXCEL_PATH` | which workbook a city resolves to, and which base slots it must cover. **Moved out of `api/config.py`** — resolving a city to a path is domain knowledge the repository needs, and while it lived under `api/` the repository could not read it without importing the web package (`tests/test_architecture.py` forbids that). `api/config.py` re-exports every name, so its importers are unchanged |
| `repository.py` | `OntologyRepository`, `repository` (process singleton) | `menu_data(city)` → `(df, pools)`; `filtered_menu_data(city, source_pools)` (F5); `nonveg_items(city)`; `item_names(city)`; `rules_for_city(city)`; `reset()`; `cache_sizes()`. **Caches keyed by resolved PATH, not city name** (every `AVAILABLE_CITIES` city has its own workbook now, so the sharing case is a city with no file, which reads Bangalore's); the F5 cache by `(path, tokens)` because tokens alone would hand a Pune client Bangalore's `common` pool. `filtered_menu_data` takes `source_pools` as an argument rather than reading Supabase — this layer no longer touches the DB. `api.app.reset_caches()` delegates to `reset()`; **never poke the old `_menu_data_by_path` / `_filtered_cache` globals, they are gone** and setting them silently loses test isolation (a test in `test_architecture.py` fails if any test file names them) |

### 4.5c `src/application/` — orchestration (neither HTTP nor pure domain)
Extracted from `api/app.py`, which had absorbed the application layer: 39% of its
2,206 lines were not HTTP. Everything here is Flask-free and DB-free, so it needs
no request context to test.

| File | Key symbols | Role |
|---|---|---|
| `constant_items.py` | `_resolve_constant_items`, `_validate_constant_values`, `_canonical_item_name`, `_exclusive_siblings`, `_slot_item_names` | resolve a client's `constant_items` pins; a pin naming a real dish narrows that cell's candidates (so every other rule still sees it), one naming a dish the city lacks is stamped post-solve |
| `horizon.py` | `_weekdays_from`, `_filter_dates_by_working_days`, `_client_base_slots` | which dates a plan covers (incl. `serve_weekends`, per-client `working_days`) and which base slots a counter runs |
| `history.py` | `_build_history_context`, `_HISTORY_WINDOW_DAYS`, `_HISTORY_WINDOW_SLACK_DAYS` | banned-item + rice-bread maps from saved history; the widened query window lives here because how far back to look follows from the cooldown rules. Returns a 4-tuple — also `recency_by_item` ({item_base: days-since-last-served}, staples excluded) for the solver's soft freshness objective (note 24) |
| `presentation.py` | `_build_item_color_lookup`, `_enrich_history_plan` | turn stored history rows back into something the planner renders (item colours, per-day theme) |

**Still in `api/app.py`, deliberately**: the solve-input assembly
(`SolverInputs`, `_prepare_solver_inputs`, `_build_solver_config`,
`_rules_and_skip_for_client`, `_run_preflight`, …). It needs 27 external names
including `get_worker_count` and the repository, so moving it is dependency
*injection*, a design step rather than a relocation — not something to rush.

### 4.6 `src/` top-level
- `constants.py` — `REPEATABLE_ITEM_FLAGS_BY_SLOT` + `repeatable_row(row, base_slot)` (staple dishes: the SAME dish may recur daily in that slot, exempt from `unique_items` and the item-cooldown ban — the 20-day window governs ordinary dishes, not staples; keyed **by slot** because `is_tandoor` also marks tandoor breads, and a flat flag list let butter naan repeat all week in `bread`), `BASE_SLOT_NAMES`, `CONST_SLOTS`, `DEFAULT_OFF_SLOTS` (selectable-but-off-by-default: `curd`/`curd_rice` + combos), `COMBO_CATEGORIES` + `combo_minority_count` (dal_rasam/sambar_rasam/dal_sambar day-split), `DISPLAY_SLOT_NAME`, `DISPLAY_SLOT_ORDER` (single canonical order for the config editor + rendered menu — `slot_sort_key` and `slot_editor` both rank by it; welcome_drink→…→dessert→other veg→**nonveg_main last**, white_rice interleaved after rice), `NONVEG_PROTEINS`/`NONVEG_SLOT` (non-veg dishes may appear only in `nonveg_main`). Every combo key must also be in `EXEMPT_FROM_CUISINE` (its minority component is often a different cuisine that would otherwise be theme-filtered off on off-theme days).
- `db.py` — `get_supabase()` (thread-safe singleton). Reads credentials + timeout from `src/settings.py`; **no longer imports `streamlit` or `api.config`** (both were layering inversions papered over with try/except).
- `log_names.py` — `APP_LOGGER_NAME = 'api.app'`: the name operator-facing application warnings are emitted under, **pinned rather than derived from `__name__`**. Moving `_validate_constant_values` out of `api/app.py` silently changed its warnings' origin to `src.application.constant_items`; log records are observable output, so anything filtering on `api.app` stopped matching (and three tests failed while a fourth passed vacuously). Purely a string — `src/` still imports nothing from `api/`. `test_architecture.py` pins it, so renaming it is a deliberate act with a failing test rather than a refactor side effect.
- `settings.py` — the shared leaf: `SUPABASE_TIMEOUT_SECONDS`, `resolve_supabase_credentials()` (env only). Imports nothing of ours. The Streamlit entrypoint bridges `st.secrets` into the environment (`app.py::_bridge_streamlit_secrets`) so a `secrets.toml` deployment keeps working with the dependency pointing downward.

---

## 5. UI layer

### `ui/`
- `api_client.py` → `MenuApiClient` (HTTP wrapper used by Streamlit; no auth — endpoints are public).
- `formatters.py` → `format_item_html(item, is_nonveg=…)`, `nonveg_slots_from_solution`, `display_label_for_slot_id`, `THEME_TAG_COLORS`, `THEME_ICONS`.
- `app.py` planner: **a failed generation clears `plan_blocks` first.** It used to `st.error(...)` and fall through, so the previous client's table stayed on screen under the new client's name and city — an Amadeus Pune header above a Bangalore counter's chicken/egg dishes, with stale Days / Slots / Total-items cards. The single-counter path now stores an `source: "error"` block (as the multi path already did), reruns, and the display short-circuits on it instead of rendering an empty table with live Save / Download buttons.
- `app.py` planner: sidebar has a single-select **City** filter (default "All", options = cities present among clients) that narrows the Client picker via `/clients` `clients_detail`; menu table renders non-veg dishes red (`.item-nonveg`, driven off `primary_protein` incl. egg); **Download Excel** (openpyxl, `_plan_xlsx`) — one sheet per counter, bold bordered headers, red non-veg; filename `menu_<client>_<date-range>.xlsx` (`_download_filename`). A **City** metric card shows the client's city.

### `customisation/` (Streamlit editors) — Pulse light theme
- `main.py` → `render_customisation_editor` (dispatcher). Stepped flow: (1) **pick city first → then a client of that city** (or create a new one in that city) → (2) Single vs Multi Cuisine Counter (+ number of counters) → (3) per-counter config. Step 1's City select sits above the client picker and filters the existing-client list to that city (via `list_clients_with_city`, same shape as the planner's sidebar); creating a client uses the City select as the new client's city (no filter). One City control — an existing client's city is not changed here (it equals the filter); a legacy null-city client is not reachable via the filter. Builds the `counters` + `city` payload for `POST /client` and `PUT /client-config`. Step 1's existing-vs-new choice is **two `st.tabs`** (Existing client / ➕ New client); Streamlit tabs are visual-only, so a name typed on the New tab is the "create" signal, else the selected existing client is edited. One param: `launch_mode` — a client created here while it is set is flagged `is_launch_site=True`, **and the existing-client picker lists launch sites only** (filtered via `api.list_clients_with_city()`'s `is_launch_site`, with a "🚀 Launch view" caption; falls back to the full list if that call fails). The editor UI is otherwise identical for launch and non-launch clients. **Multi-counter clients get a "Shared categories" section** (a toggle + a multiselect of base slots present on 2+ counters) that writes `shared_categories` into the create/update payload — the cross-counter common-category sync (note 22). The **Item Pools** multiselect (F5 `source_pools`) labels each ontology pool token by the app client whose name it matches: `pool_token_client_labels(api, city, tokens)` word-subset-matches a token against the selected city's client names (a token matching no client is title-cased), so the operator picks "Cloudera" rather than the raw `cloudera` token.
- **Launch view / Ops Client View (feature F)** — `app.py`: a **"Launch sites" sidebar toggle above City, default OFF**. On → **Launch view**: the picker lists launch sites only (City still narrows), "🚀 Launch view" caption, picker relabelled "Launch site", and a **"Move to Ops Client View" button** demotes the selected launch site (`update_client_config` PUT `is_launch_site=False`, then cache clear + rerun). Off → **Ops Client View**: the picker lists the operational (non-launch) clients only — the two views partition the client set. The rest of the UI is **identical** in both (same dark theme, header, full-page Edit-Logic editor) — a site is planned/edited exactly like any other client. A client created via Edit Logic while the toggle is on is flagged a launch site (`render_customisation_editor(launch_mode=…)` → `create_client(is_launch_site=…)`). No separate launch layout/theme — deliberately the same UI/UX.
- `counter_editor.py` → `render_counter_editor` — composes the 3 panels for one counter; returns `{name, categories, slot_counts, theme_map}`.
- `slot_editor.py`, `multi_slot_editor.py`, `theme_editor.py` — per-concern panels (categories / frequency / day themes), counter-scoped via a `key_prefix`.
- `pulse.py` → `PULSE_EDITOR_CSS`, `PULSE_THEME_COLORS` — the light (OP Lens) design tokens; injected by the editor. `app.py` skips the dark `ui/styles.py` `STYLES` while `view == "editor"`.

---

## 6. Data & config files

| Path | Purpose |
|---|---|
| `ikigai_masala-main/data/raw/city_items/<city>.xlsx` | **per-city item ontologies** — one file per city slug, selected from `clients.city`. `bangalore.xlsx` is the master **rule-ready** ontology (4,321 items; item_id key; cols incl. course_type, sub_category, cuisine_family, item_color, key_ingredient, primary_protein, is_premium_veg/_veg_dry, is_chinese_*/is_continental_*/is_*_biryani, is_liquid_dessert, welcome-drink subtypes (is_lassi/is_milkshake/is_soda_drink/is_cooler_drink/… + drink_rule_group), is_lentil_based/is_whole_legume_based/is_legume_salad, is_pulao, is_grill, is_dosa(_family), is_oil_based_bread, is_black_chana_gravy/is_kabuli_chana_gravy, plus quality signals classification_confidence/is_rule_ready/rule_data_note) and the fallback for any city without its own file. `pune.xlsx` = 507 items, all-veg (Corning Chakan's Maharashtrian menu took it from 377; see `scripts/import_corning_pune_menu.py`). `chennai.xlsx` = 617 items, **has non-veg** (incl. fish) and **no welcome drinks at all**. `ncr.xlsx` = 1,629 items, North Indian, **has non-veg** (incl. 2 fish) and welcome drinks; arrived already in the master schema from a mapping pipeline (see `docs/ncr_client_logic.md`), so normalisation was a light pass. (Chennai/Pune/NCR all dropped rows named for a category, not a dish, via `scripts/remove_generic_rows.py` — `sweet`/`veg_gravy`/`salad`/…; the client chose removal over renaming.) `hyderabad.xlsx` = 6,260 items and is the odd one out: it is **SEEDED FROM `bangalore.xlsx`** and then adds Quest's 101 Telugu/Telangana dishes (`scripts/import_quest_hyderabad_menu.py`), because Quest's grid names 191 distinct dishes — about seventeen per slot, a `bread` row of ONE — and a standalone list that size starves in week two under the 20-day cooldown. Seeding has two consequences, both fixed rather than accepted: ~6,000 inherited rows carry BANGALORE client pool tokens, so Hyderabad had to join `FULL_POOL_CITIES` or every client narrows to `common` (960 rows, none of them Quest's); and they double the corpus the all-cities correction scripts learn from without adding a fact, which is why `complete_ontology` and `fill_item_colours` now weigh evidence per DISH rather than per row (undeduped, Hyderabad's arrival alone would have written 292 + 249 cells into the four established workbooks, every one of which was at a fixed point beforehand). All five workbooks are **134 columns**: `is_seafood` + `is_fish_dish` were added after `is_egg_dish` (see `scripts/seafood_taxonomy.py`) because Chennai is the first city with fish and the master taxonomy had only chicken + egg, and `universe` was removed (`scripts/drop_dead_columns.py`) as blank on every row in every city and read by nothing. Attribute completeness is `scripts/complete_ontology.py`'s job — six client menu imports left 1,773 rows with **no `is_*` flag at all**, invisible to every flag-driven rule; it is down to 200. Resolution + caching: `api/config.py::city_excel_path` / `city_required_slots`, `api/app.py::_get_menu_data(city)` |
| `ikigai_masala-main/data/raw/source_workbooks/` | **every client-supplied workbook in one place** — raw item lists, the Pune rulebook (70 rules) and the Bangalore rulebook (`bangalore_client_logics.xlsx`, 158 client logics), Chennai's sample menu, Bangalore's sample history. Inputs, not runtime data: nothing in the app reads this directory, the normaliser reads *from* it to produce `city_items/<city>.xlsx`. Committed because the derived artefacts cite them, and without the sources a rule's reasoning is unverifiable. Only Bangalore's samples were committed before; Pune's and Chennai's raw lists, the rulebook and the sample menu existed solely as chat attachments and would have been lost. See its README for the per-file table and the add-a-city steps |
| `ikigai_masala-main/data/raw/city_items/ontology_categories.json` | which base slots each city's ontology is **expected** to cover. `PoolBuilder.build_pools(df, required_slots=…)` raises on an empty required slot (a mapping regression), and a city list legitimately covers only what that city serves — Pune has no non-veg/sambar/rasam/starter/soup. A city absent from this file requires every mandatory slot (the pre-per-city behaviour) |
| `ikigai_masala-main/data/configs/city_rules/<city>.json` | **per-city rulesets** (one file per city: `bangalore.json` = the reference ruleset, `pune.json` = a standalone transcription of the Pune rulebook — see `docs/pune_rulebook.md`; `chennai.json` = standalone but NOT a rulebook transcription (none was supplied — it is the engine skeleton plus caps provable from the item list, see `docs/chennai_client_logic.md`); `ncr.json` extends Bangalore, has its **own** item list (`ncr.xlsx`, see `docs/ncr_client_logic.md`) and adds two NCR-specific rules, the across/within pair of note 23 — `ncr_south_bread_cadence` (`selector_history_window`, 15 days over `cuisine_family: south_indian` @ bread) so a counter with a south-themed weekday serves a dosa/idli about once every three weeks instead of every week, plus `ncr_south_bread_weekly_max` (`selector_frequency max: 1`, same selector) because the window only reads history and **the bread cuisine lock runs only on south/north days** — biryani/chinese/continental/mix days take other branches that never touch bread, so a south bread was also landing on the biryani day. When the family is banned the cuisine lock finds nothing south and falls back to the full bread pool, so the day serves a chapati and can never go INFEASIBLE; `hyderabad.json` extends Bangalore with no overrides — but it no longer shares Bangalore's item list: `hyderabad.xlsx` is its own, SEEDED from Bangalore's plus Quest's 101 Telugu/Telangana dishes). A file may `"extends": "<city>"` to inherit + override by rule `name` (and `"disable": [names]`). Loaded via `MenuRuleLoader.load_for_city(city)`; `CITY_RULES_DIR` + `DEFAULT_CITY='bangalore'` (the fallback for any city without its own file) live in `menu_rule_loader.py`. The API caches one ruleset per client's `city` |
| `ikigai_masala-main/data/configs/rule_library.json` | **the shelf a client file picks from** — reusable rule bodies referenced by `"use": [{"ref": ..., "as": ..., "with": {...}}]`, where `with` shallow-merges over the template per key (same semantics as a city `extends` override). Exists because the same body was copy-pasted across clients: 'paneer once a week' appeared verbatim in ELEVEN files. A component is only worth adding when a SECOND client needs it; one client with an unusual requirement writes an ordinary rule in its own file. NB `bread_chapati_only` selects on **`is_plain_phulka_chapathi`, not on the name** — it used to match `name_contains: [chapati, chapatti, phulka]`, which the flag being unreliable forced, and the cost surfaced the moment Citrix was wired: Tuesday's 'chapati' came back `appam_chapati`, a combination cell a menu import split badly. `scripts/bread_form_flags.py` made the flag definitional in both directions, so the column is now the trustworthy selector |
| `ikigai_masala-main/data/configs/clients/<slug>.json` | **one file per client** (36 today), each a single top-level `{"<clients.name>": {disable, rules, constant_items}}`. The **client name lives inside the file; the filename is cosmetic** — `Booking.com`, `L&T` and `ToastTab CHN` cannot survive a filesystem-safe slug, and the lookup is exact-match where a mismatch is silent. `MenuRuleLoader._read_client_blob()` merges the directory and **raises** if a client appears in two files (silent last-one-wins is the same bug class as a name typo). Falls back to the retired single `client_rules.json` when the directory is absent, so an un-migrated deployment still works; `CLIENT_RULES_DIR` overrides the location. Regenerate/verify the split with `scripts/split_client_rules.py [--check]`. Per-client overrides keyed by client name: `{disable, rules, constant_items}` (or legacy rule list); merged by `MenuRuleLoader.load_for_client()`. Keys starting with `_` are documentation everywhere, including inside `constant_items`. Bangalore clients are mapped in `docs/client_logics.md`, Pune's in `docs/pune_client_logic.md`, the 5 NCR sites in `docs/ncr_client_logic.md` (paneer/chicken/egg/potato frequencies + non-veg day restrictions) |
| `ikigai_masala-main/scripts/pune_flag_corrections.py` | nine flag fills the raw Pune workbook left at 0 (`is_black_chana_gravy` on the one black-chana gravy; `is_leafy_based_dish` on the palak/methi/coriander/mint dishes). Both R14 and R31 were silently inert without them. Committed and idempotent because re-importing a fresh workbook through the normaliser drops the corrections — re-run it after any re-import; `test_pune_rules.py::test_flag_corrections_are_applied` fails if they are missing |
| `ikigai_masala-main/scripts/audit_course_types.py` + `course_type_corrections.py` | `course_type_corrections.py` also carries `ATTRIBUTE_CORRECTIONS`, for a row whose **form** was recorded wrongly rather than its course: `pav` was filed `sub_category: plain_chapatti/phulka` with `key_ingredient: pav` (the name's own first word — the mapping pipeline's fingerprint) in all three cities that carry it, and Pune and NCR carried `is_plain_phulka_chapathi` from it. That flag is what Pune's R36 staple exemption selects on, and an exemption is permission to repeat EVERY DAY — a pav is a leavened roll served alongside a bhaji once a week, and the client confirmed it is not a plain phulka or chapathi. It is refined flour, so it now counts against `maida_bread_weekly` (`is_maida_bread`, `max: 1`) and its `key_ingredient` is the `maida` 33 other bread rows use. `pav_/_bun` is a new sub_category written to every city with the dish, so the "Pune's values are a subset of Bangalore's" assertion still holds, and nothing implies a flag from it (3 rows is under `MIN_SUPPORT`) — which is the point, the old value is what kept re-deriving the wrong flag.  **a dish must sit in its own category.** `course_type` picks the slot pool, so a misfile makes a dish servable in the wrong position — `semiya_pal_payasam` was `veg_gravy` (a dessert served as a gravy) and `moong_dal_dosa` was `dal` while all 37 other dosas are `bread`. No rule can catch this: nothing in the engine knows what a dish *is*, only what its columns say. It also carries a **structural** check names cannot do: a row whose `primary_protein` is non-veg while its `course_type` is not `nonveg_main` is **unservable** — `_nonveg_mask` drops it from its own pool and it never joins nonveg_main (`egg_fried_rice` was `rice`+`egg`; `urandai_kuzhambu` was a veg gravy wrongly tagged `egg`, fixed the other way by clearing the protein rather than making a lentil-dumpling gravy non-veg). The auditor compares each dish's NAME against its filing, matching whole `_`-tokens rather than substrings (substrings gave 238 false hits for Bangalore — `kadai` contains `adai`, `kolhapuri` contains `puri`; tokens give 9). What survives is silenced two ways: `LEGITIMATE_PAIRS` for by-design overlaps (a chicken biryani belongs in `nonveg_main`; `kesari_bath` is a sweet named for a rice dish) and `ADJUDICATED` for individual reviewed rows, each carrying its reason. Anything else exits non-zero and `test_course_type_audit.py` fails the build — including a planted-misfile test so the guard cannot pass vacuously |
| `ikigai_masala-main/scripts/remove_generic_rows.py` | drops rows named for a *category* not a dish, or for a **component** of one: `curd_base` is the base a raita is built FROM, and it sat in `curd_side` in three cities with `sub_category: curd` and `key_ingredient: yogurt`, so it looked servable to every diagnostic while a menu printing "Curd Base" as the day's yogurt side says nothing (client-confirmed). Also `sweet`/`veg_gravy`/`salad`/`chutney`/… — Chennai 10, Pune 3, NCR 10, Bangalore 1; NCR's include its only `rasam`/`sambar` rows so it carries no such station, but `curd`/`papad`/`pickle` are kept as fixed condiments). A menu printing "Sweet" is useless and no colour/ingredient/variety rule can reason about an unidentifiable dish. Client chose removal over renaming (D3); `dry_sweet`/`sweet` were on live ToastTab menus, so those sample rows are now unreproducible — a deliberate trade. None starves a required slot (smallest affected pool is Chennai soup, 8). Idempotent; `test_generic_rows.py` fails if any reappear |
| `ikigai_masala-main/scripts/dessert_cuisine_corrections.py` | fixes `cuisine_family` on desserts the import defaulted to `north_indian` (221/249 Bangalore, 28/32 Chennai, 43/45 Pune). Token-keyed by dish family, scoped to `course_type == dessert`: the payasam/kesari/mysore-pak/holige/sweet-pongal families → `south_indian`; western bakery (cake/brownie/muffin/custard/ice_cream) → `continental`, with `milk_cake`/`ajmeri_milk_cake` excluded (mawa sweets). East (Bengali) and West (Gujarati/Maharashtrian) sweets stay `north_indian` **by design** — the vocabulary has no such bucket and no theme maps to one. Inert today (desserts are theme-exempt) but a landmine (D1); idempotent, `test_dessert_cuisine.py` |
| `ikigai_masala-main/scripts/seafood_taxonomy.py` | adds `is_seafood` + `is_fish_dish` to **every** city workbook (a column absent from the reference list cannot exist in any city) and reclassifies seafood rows off the chicken buckets the import filed them under: `sub_category` `chicken_south_coastal`→`fish_south_coastal` (suffix kept), `key_ingredient` chicken→fish (a live `ingredient_ban_rule` bug — it matches key_ingredient **and** primary_protein, so banning chicken also banned the fish), chicken-only flags cleared (`fish_kuzhambu` held `is_south_chicken_gravy`), and `fish_65`/`fish_roast` moved to `south_indian` to match the master's own `chicken_65`. **`is_fish_dish` is definitional in BOTH directions** — set for `FISH_PROTEINS` and cleared otherwise — because the Chennai menu bank import cloned a fish template for `prawn_thokku` and `prawns_biryani`, putting a prawn inside every rule about fish (prawn is seafood, and is not a fish). Same argument as the course mirrors in `complete_ontology.py`: a flag that is only ever set drifts. Idempotent; re-run after any re-import, same as the Pune corrections. `test_seafood_taxonomy.py` fails if the corrections are missing |
| `ikigai_masala-main/scripts/ncr_cuisine_corrections.py` | **NCR only.** The mapping pipeline mislabeled 24 plainly-Indian savoury dishes `cuisine_family=continental` — 17 North Indian chicken curries in `nonveg_main` (each already `sub_category=chicken_north_*`, so the row contradicted itself) and 7 Indian chaat/pakora `starter`s. `ThemeSlotFilterRule` hides a continental dish on every non-continental day and no NCR client runs one, so all 24 were silently **unservable** (Airtel Plot 5 went INFEASIBLE on a starved starter slot). Retags exactly those two cuisine-main slots → `north_indian`; genuinely-continental veg_dry/soup rows are left alone. Idempotent; `test_ncr_cuisine.py` |
| `ikigai_masala-main/scripts/import_corning_pune_menu.py` | Corning Chakan (Pune)'s nine-week grid → Pune. **The first client menu imported into a city other than Bangalore or Chennai**, and the first Maharashtrian list (amti, patodi, thecha, matki, bharali wangi, fodanich waran): +130 dishes, Pune 377 → 507, and the `chutney` (0 → 13) and `papad` (0 → 6) const-slot pools it had none of. Lunch + dinner only — breakfast, evening and midnight snacks are separate services. Four source-specific readings: the **salad block is a salad BAR**, so its five continuation rows are dropped (their `+` keys are absent from `CATEGORY_MAP`, which is what skips them) and the labelled row is filtered against `SALAD_BAR_COMPONENTS`, its composed chaats re-filed to `starter` (Pune had 7); **three printed rows serve two slots each** and split by name (`DAL` holds a rasam and a soup, `DESSERT/SOUP` is half sweets, `SOUP / TETRA PACK` mixes soups with lassi/buttermilk/masala milk); **the chutney row names the ingredient**, writing `Tomato` and `Tomato Chutney` for the same thing, so the category word is appended when missing (otherwise the ontology gains a row called `tomato`); and **one unlabelled Independence Day block** below the grid whose course comes from the dish NAME, never from position, with `FESTIVAL_COURSES` adjudicating the four `course_from_name` cannot place. Pool token is `common` per the Pune convention — Pune is not in `FULL_POOL_CITIES`, so a dish tagged `Corning Chakan` would be invisible to every Pune client. Idempotent; `test_corning_pune_import.py` |
| `ikigai_masala-main/scripts/marathi_ingredient_names.py` | **all cities.** Gives the Marathi-named dishes a `key_ingredient` — `dodka` = ridge gourd, `shepu` = dill, `gawar` = cluster beans, `matki` = moth bean, `chavali` = cowpea. `complete_ontology.py` correctly refused these: a token vote can only propose a value the ontology already uses for dishes with that word in the name, and no row had ever carried them. A dictionary settles what a data vote cannot. Every value is one the ontology **already uses**, and where its spelling is the shorter one that is what gets written (`moth` not `moth_bean`, `yam` not `elephant_yam`, `leafy_greens` not `mustard_greens`) — a new synonym would be invisible to the rules naming the old one. `PHRASES` is checked first because the split lentil and the whole legume are different ingredients: matching one token at a time filed `chana_chaat_salad` as `chana_dal`. `kala chana` → `chickpea` deliberately, that being a value the client's protein list names. Also folds the `flaxiseed` typo. Matters because `attribute_grouping` groups the sambar/dal slots by this column, `ingredient_ban_rule` matches on it, and a blank is invisible to both. Idempotent; `test_marathi_ingredients.py` |
| `ikigai_masala-main/scripts/complete_ontology.py` | **all cities.** Fills the attributes the six client menu imports could not: those importers set only what a dish NAME supports, leaving **1,773 rows with every `is_*` flag at zero** — a dish no flag-driven rule can ever ask for, sitting in the pool and passing every diagnostic. Nothing is guessed; each channel is measured against the rows the ontology already classifies. **Cross-city dishes** — the four workbooks share most of their rows, so a dish already filled in Bangalore settles the Pune copy outright (the same argument `fill_item_colours.py` makes for colour); only unanimous dishes count, since two cities disagreeing is not evidence. **Course mirrors** — nine flags restate the row's course (every classified `dessert` carries `is_dessert`, 512/526; the misses are `kulfi`, `rava_kesari`), so the flag is set for every row of that course **and cleared from every row outside it**, which is what catches re-file residue: `moong_dal_dosa` moved `dal`→`bread` and `jodhpuri_pulao` out of `bread`, but the old course's flag stayed, so a dal rule could still pick the dosa. **Attribute implication** — a `(course, column, value)` predicting a flag in ≥5 classified rows at ≥95% implies it (`sub_category == raita` ⇒ `is_raita`); reaches 89 of 112 flags. **Token vote** for the rest, as `fill_item_colours.py` does for colour. Both channels are existential, so **exclusive pairs are derived from co-occurrence** (`is_dosa` and `is_paratha`: 50 and 105 rows, zero overlap) and the better-evidenced proposal wins — the dish's NAME beats an attribute and a later word beats an earlier one, Indian names putting the form last. Without that, `paneer_masala_dosa` drew `is_paratha` from `key_ingredient == paneer`, every classified paneer bread being a paneer paratha. **Premium is reported, never written** (`APPLY_PREMIUM = False`, `--report-premium`). `is_premium` encodes the client's stated rule (paneer / baby corn / mushroom / rich continental, matched as phrases not tokens — `baby_corn` is never a token of `baby_corn_manchurian_gravy`), but the three premium flags are the inputs to shipped COST rules — `premium_veg_gravy_exactly_one` / `premium_veg_dry_exactly_one` (`exact` counts **days**, so one premium dish a *week*) and Pune/Chennai's `premium_veg_daily_max_1`. Chennai's ruleset records the count it was calibrated against ("One premium veg gravy a week (25 eligible rows)"). Applying the client's cost definition takes Bangalore's `is_premium_gravy` from 174 to 463, at which point the weekly rule stops meaning "the week's showcase dish" and starts meaning "paneer at most once a week" — fighting `paneer_prefers_mix_south_north`, and leaving a themed day whose gravies are ALL premium forcing the cap past `exact: 1` (note 9e). That is a menu-policy decision, not a data gap, so it goes back to the client and `APPLY_PREMIUM` is the switch. `--report-mixed-veg` separately sizes the "a lot of vegetable" clause read as the catch-all `key_ingredient == mixed_vegetables` (437 more rows the ontology flags as NOT premium), also not applied. `richness_score` untouched (6,092 of 6,169 rows are 0 and nothing reads it). Runs to a **fixed point** (3 passes) because it re-learns from its own output. Monotone: a value is never overwritten and a flag only goes 0→1. Leftovers → `docs/ontology_gaps.csv`. `test_complete_ontology.py` |
| `ikigai_masala-main/scripts/drop_dead_columns.py` | **all cities.** Removes a column that is empty in every city workbook and read by nothing — today just `universe`, blank on all 8,787 rows. Verifies emptiness across EVERY city before touching any of them, so a column holding data anywhere is refused whatever the others say. Schema 135 → 134; `test_course_type_audit.py` pins the count so the change is visible rather than silent. Idempotent. `test_drop_dead_columns.py` |
| `ikigai_masala-main/scripts/expand_side_pools.py` | **all cities.** Adds 7 new veg dishes to each of `healthy_rice`/`dessert`/flavoured-chapati `bread`/`starter` in every city — the small-pool categories a rolling sweep found draining under the cooldown (Pune had 1 healthy rice, 0 starters). A fixed curated set of simple pan-India dishes absent from every city today, cloned from a plain same-category Bangalore template for the 135-col skeleton with all `is_*` flags zeroed + essentials/colour/cuisine set (so no `gulab_jamun`/`gobi_65` specifics leak in); `client` follows the city convention (`common`, or blank for NCR). Pune gains a `starter` category (added to `ontology_categories.json`). Idempotent (fixed set, skip-by-name); re-run after any re-import. `test_expand_side_pools.py` pins counts, veg-only, clean flags, schema, and the no-op re-run |
| `ikigai_masala-main/scripts/ncr_bread_misfiles.py` | **NCR only.** Twelve curries/dals/a salad were filed as `course_type=bread` by the mapping pipeline — `paneer_jaipuri` (a paneer curry, `primary_protein=paneer`) sat in the bread pool as `sub_category=flavoured_paratha`, so a counter could serve it as the day's roti. All twelve share the bad-mapping fingerprint: blank `cuisine_family` + `key_ingredient` copied from the first word of the name (`key_ingredient=jaipuri`). `audit_course_types.py` missed them because "jaipuri"/"jodhpuri" are place names, not dish words. Ten are **re-filed** to their real category (also deepening those pools); `jaipuri_paneer` is removed as a duplicate of `paneer_jaipuri` and `breads` as a category-named row. NCR bread 66 → 54. Idempotent; `test_ncr_bread_misfiles.py` |
| `ikigai_masala-main/scripts/add_ncr_sambar.py` | **NCR only.** Copies 10 diverse vegetable sambars from `bangalore.xlsx` into `ncr.xlsx` — NCR's raw list carried NO sambar (`course_type == sambar` empty), so Stryker NCR's "a sambar once every 15 days" had nothing to serve. Same 135-col schema, so a row copies verbatim; the `item_id` is reassigned beyond NCR's current max (no collision) and `client` is blanked (reachable via NCR's full-list fallback). Idempotent (skips by name); re-run after any NCR re-import. `test_ncr_sambar.py` fails if the 10 are missing. NB: serving them still needs a sambar/dal_sambar slot on the counter + the cross-week 15-day cadence rule |
| `ikigai_masala-main/scripts/split_client_rules.py` | splits the retired single `client_rules.json` into `data/configs/clients/<slug>.json`, one per client. `--check` verifies the split still matches a source file without writing (what `test_menu_rule_loader.py::TestPerClientRuleFiles` pins). Idempotent; removes files for clients no longer in the source |
| `ikigai_masala-main/scripts/menu_import.py` | **the shared menu-import machinery** — not a correction script, the library the per-client importers are built on. Holds the three passes every earlier city import used (language → similar items → unique): `SPELLING` (the misspellings real client menus are typed with, as whole-token lookarounds because `\b` does not fire next to `_`), `to_item`, the evidence-based `fold_similar` + `_existing_twin` (a merge needs the ontology's own token frequency behind it; `KEEP_APART` for look-alikes that are different dishes, `SAME_DISH` for two spellings of one), `vocab_from` (excludes the import's own rows or the second run un-folds the first), one-dish-one-category filing via `COURSE_PRIORITY` + `refile_lentils`, `cuisine_for`/`protein_for`, and the `build`/`run_import` row builder. A client importer supplies only its workbook grid and its label → slot map, via `ImportSpec` |
| `ikigai_masala-main/scripts/import_booking_menu.py` | Booking.com's 3-month Lunch/Dinner grid → Bangalore. Where `infused_water` + `nonveg_soup` came from. Idempotent; `test_booking_import.py` |
| `ikigai_masala-main/scripts/import_stripe_menu.py` | Stripe's two sample weeks → Bangalore (94 dishes, 50 rows re-tagged). **Plated blocks only** — the salad bar's components and the DIY sandwich station are things a diner assembles, not solver slots; `Veg Soup` and `Beverage` are taken from the salad-bar block because they ARE slots. Two source defects are handled by detection rather than assumption: the July workbook's salad-bar block lost a row so its dishes sit one row above their labels (`_aligned` probes that `Veg Soup` holds a soup and `Beverage` a drink, retries shifted, and **skips the block** if neither lines up — a wrong import is worse than a missing one), and the day-theme strip ("Monday ( Rajasthan)", "Thursday-chinese") is not a row of dishes (left in, its five strings imported as five salads, because the plated salad row beneath it carries no label). Stripe's mutton rule stays unconfigured — Bangalore has none and neither does Stripe's own menu. Idempotent; `test_stripe_import.py` |
| `ikigai_masala-main/scripts/misspelled_protein_names.py` | **all cities.** Four rows named for chicken/mutton while every attribute said vegetarian, so the solver served them from the veg `starter`/`rice`/`veg_gravy` pools and the menu printed a meat name to a vegetarian. **The misspelling is what hid them**: `audit_course_types.py` matches whole `_`-tokens of a name against real dish words, and `chciken`/`chivken`/`muton` are not words — and with `chciken` in the vocabulary as a "real" token the importer's own fold read `chciken_mulligatawny` and `chicken_mulligatawny` as two dishes. Each row adjudicated on its own evidence: **rename** `hoskote_chciken_biryani`→`hoskote_veg_biryani` (four columns say veg biryani), **remove** `chciken_kebab` + `hyderabadi_chivken` (spelled correctly each duplicates a properly-filed dish), **re-file** `muton_curry`→`mutton_curry` into `nonveg_main` with `primary_protein=mutton` (NCR's only mutton). Idempotent; `test_misspelled_protein_names.py` |
| `ikigai_masala-main/scripts/nonveg_structural_flags.py` | **all cities.** A non-veg dish carrying no *form* flag can never be served: `nonveg_main_daily_pair` spends both cells of a 2-4 slot counter on **one `is_nonveg_dry` + one north/south chicken gravy every day**, so a dish with neither sits in the pool, passes every diagnostic, and is simply never chosen. Invisible until something forces it — Stripe's `min: 1` fish rule did, leaving the composition one cell for two components and turning the counter INFEASIBLE with nothing pointing at the cause. **111 Bangalore + 5 NCR rows** had the hole (most predating any import; the menu imports added 18 more by zeroing every `is_*` on a cloned template). Fills 69 from the dish NAME — `biryani` → `is_nonveg_biryani`+`is_biryani_item`; a gravy word (curry/masala/korma/butter/kadai/do-pyaza/…) → `is_nonveg_gravy` plus the regional chicken-gravy flag chosen by `cuisine_family`; a dry word (dry/fry/roast/sukka/kebab/tikka/65/tandoori/pepper/…) → `is_nonveg_dry`; both words → gravy, that being the dish's form. `STYLE_OVERRIDES` carries the four whose name is silent but whose printed row says which (Stripe prints "Semi Dry or Dry" and "Curry or Main Course" as separate rows — `ImportSpec.style_by_label` feeds the same evidence into future imports). Scoped to `nonveg_main`: `shorba` is deliberately not a gravy word, since Bangalore files its shorbas as `nonveg_soup`. **17 rows are left alone and reported** (`afghani_chicken`, `kolhapuri_chicken` — a place plus a protein says nothing about form); guessing would put a dish on the plate in the wrong role. `mutton_curry` gets `is_nonveg_gravy` but no chicken flag, so NCR's only mutton still needs a 5-slot station or its own composition. Also **clears the regional CHICKEN flags off any row whose `primary_protein` is not chicken** (`CHICKEN_ONLY_FLAGS`): `egg_kurma` carried `is_south_chicken_gravy` in three cities, so ICON Chn's "a chicken gravy on Tuesday" came back as an egg kurma and every 'one dry plus one chicken gravy' composition could lose its chicken. 16 rows, including twelve NCR soya/veg keemas whose `primary_protein: mutton` is left alone — whether a vegetarian keema belongs on a non-veg counter is a client question, not a column fix. Idempotent; `test_nonveg_structural_flags.py` |
| `ikigai_masala-main/scripts/canonical_dish_spellings.py` | **all cities.** One dish, one spelling: `chana` (153 rows) vs `channa` (16), `kadhi` (13) vs `kadi` (2) — the same word twice, so a `name_contains` selector misses half the family AND a menu import de-stabilises (the importer maps `channa`→`chana`, but while both live in the workbook the fold reads them as two real words and adds a second row — Booking went 0 → 9 new dishes on a re-run for exactly this). 23 renames; three colliding pairs were compared column-by-column, confirmed the same dish, and merged with their `client` lists folded together (`DUPLICATES`). A rename that would collide is otherwise reported and skipped, never applied. `chapatti` (36) → `chapati` (41) was left split for a while ("both are ordinary transliterations and the name is printed on a menu") and the split then produced the cost at scale: **eight pairs of the identical dish**, every `chapatti` row the fully-attributed master and every `chapati` row a bare import stub beside it, so `plain_chapati` sat in the pool with `is_plain_phulka_chapathi = 0` while `plain_chapatti` carried it — a chapati rule saw one and not the other, and a bread slot could serve "Plain Chapati" Monday and "Plain Chapatti" Tuesday with `unique_items` none the wiser. `chapati` wins (the client's own spelling, and the majority across cities); the attributed row survives each merge. Bare `chapati` is **not** merged into `plain_chapati` and is recorded in `KNOWN_SPLITS`: it looked like the same case (same dish, same sub_category) but it is a naming GRANULARITY question, not a spelling one, and merging it broke import stability — Stripe's printed menu says "Chapati", so a re-import added the row straight back, and teaching the importer an alias would be wrong for Pune and Chennai, whose row IS `chapati`. Also folds two pure typos (`malasa`→`masala`, `tempared`→`tempered`), which matters because Citrix's welcome drink is buttermilk every day and `malasa_buttermilk` would be printed weekly. `KNOWN_SPLITS` still records splits looked at and left alone. **Run before any client menu import.** Idempotent; `test_canonical_spellings.py` |
| `ikigai_masala-main/scripts/bread_form_flags.py` | **all cities.** `is_plain_phulka_chapathi` in **both directions**, the same argument `seafood_taxonomy.py` makes for `is_fish_dish`: a flag that is only ever set drifts. Four clients state their bread rule as "chapati only" (AT&T, Booking, Clario, Citrix) and Pune's R36 makes the plain chapati a staple, so this one column decides what lands in a bread slot — and it was wrong both ways. **Set on dishes that are not chapatis**: 19 NCR rows from the mapping pipeline (`dhaba_chicken_curry`, `kolhapuri_chicken`, `egg_curry_masala` — not even breads — plus `jodhpuri_pulao`, `pav_bhaji`, `pao`, `fried_idli`, `bhelpuri`, four poories, three kulchas, two parathas) and `amras_puri`, `bhel_puri`, `ragi_mudde` in Bangalore, so a "chapati only" rule could serve a puri or a millet ball. **Absent from dishes that are**: every `chapati`-spelled row in every city, the imports and `expand_side_pools.py` having left the column blank. **Inconsistent across the roti family**: `akki`/`ragi`/`jowar`/`bajra`/`rumali`/`tandoori` rotis unflagged while `joleda`/`rava`/`coorg`/`romali`/`tandoor_*` rotis were flagged — the ontology's own majority (and Clario's menu, which lists Rumali Roti and Akki Rotti as *special* breads) says a millet, rava, tandoor or rumali roti is not a chapati, and that is the reading applied. Definition, from the NAME: a chapati/phulka token, or a `roti` token with no other-family qualifier, on a `course_type == bread` row; a competing bread form or another course in the name clears it (which is what removes the `*_chapati_dosa` combination rows a menu import split badly). 66 corrections → Bangalore 62 / NCR 14 / Chennai 12 / Pune 9. Runs after the spelling fold, since it reads names. **Widening the flag forced Pune's R36 and Chennai's staple exemption off it**: those selected on this flag, and taking Pune from 2 flagged rows to 9 would have licensed a beetroot chapati every day as a side effect of a data fix. Both now name the two plain dishes (`any_of` on `item`) — not `sub_category` either, which holds `garlic_chapati` here and 34 Bangalore rows of which several are plainly flavoured. Idempotent; `test_bread_form_flags.py` |
| `ikigai_masala-main/scripts/fill_cuisine_family.py` | **all cities.** Fills `cuisine_family`, whose blanks are a BAN rather than a gap: `ThemeSlotFilterRule` narrows a cuisine-main slot with `pool[cuisine == target]`, so a blank matches no target and the dish is dropped from every themed day in rice/veg_gravy/veg_dry/starter/nonveg_main. Four cities were 100% filled; **NCR was 62.9% blank — 1,000 rows, 613 in cuisine-main slots** — while every NCR client themes most of its weekdays, so those 613 sat in the pool, passed every diagnostic and were filtered out before the solver saw them. 767 filled; a north-themed day's `veg_gravy` goes 106 → 462 candidates and `nonveg_main` 38 → 135. **The vote may only ever propose north_indian or south_indian**: chinese and continental make a dish appear ONLY on their own theme day, so guessing one is strictly worse than a blank (which at least survives a mix day), and a row carrying an `is_chinese_*`/`is_continental_*` flag is skipped and reported. Welcome drinks too — the corpus splits 331 `drink` / 139 `north_indian` for that course. Thresholds measured on held-out data (6 rows/95% = 96.5% accuracy; one row in 1,370 wrongly called continental, which the value restriction takes to zero). Two tiers beat the plain vote and both exist for a specific defect: **the sub_category NAMES the region** (`chicken_north_masala` is north — reading the field, not voting; needed because Bangalore's 53 continental-tagged such rows drag the corpus to 46% north and were blocking 64 NCR rows), and **the city's own convention beats the corpus** (97.5% vs 96.5% held out per city — `mixed_veg_curry` is 77% north corpus-wide and correctly refused as regionless, but 97% north within NCR, Bangalore's south Indian cooking having diluted a North Indian city). Monotone, idempotent, runs to a fixed point; the 233 undecidable rows go to `docs/cuisines_to_confirm.csv`. `tests/data/test_cuisine_family.py` |
| `ikigai_masala-main/scripts/fill_item_colours.py` | fills `item_color`, whose blanks are not neutral: `MenuSolver._add_color_constraints` clamps a day's required distinct colours to the number PRESENT among the candidates, so 2,440 uncoloured rows quietly relaxed the colour rule everywhere. Four tiers — the same dish coloured in another city, a token vote, an attribute the coloured rows agree about (`course_type`+`key_ingredient`, `key_ingredient`), and a form flag (`is_leafy_based_dish` is green in 98% of its 353 rows) — and still **NOT** the course-type majority. Thresholds are chosen by **held-out measurement** rather than judgement (train on 80% of the 6,445 coloured rows, predict the rest): 12 rows/80% scored 94% accuracy but filled ZERO of the remaining blanks, so the vote runs at 6/70 = 90% accuracy at 30% coverage. Runs to a fixed point (a filled colour is evidence for the next pass — without the loop one invocation left 77 rows a second would have taken, so the chain did not converge). 744 filled; the remaining 1,696 go to `docs/colours_to_confirm_by_family.csv` as **455 greedy-set-cover questions** rather than 1,696 rows, ordered so answering the top 25 colours half the backlog. The cover is computed over `sorted()` keys because `_question_tokens` returns a set and set iteration is hash-salted per process — an unsorted scan made the committed report change every run. Since the enriched merge the column is complete in every city, and the last three blanks are in **`ADJUDICATED`** — `(item, course_type) -> (colour, reason)` for a dish no tier can decide. All three exist because the client's enrichment pass DROPPED them as generic and we deliberately kept them (`mixed_veg`/`sprouts` are dish families not slot names; `mixed_fruit_crush` is a welcome drink distinct from the `mixed_fruit_custard` dessert), so no enriched colour crosses over — and each falls just short of a MEASURED threshold, which is a reason to give a verdict rather than loosen one: `(veg_dry, mixed_vegetables)` is green in 79% of 117 rows against `MIN_ATTR_AGREEMENT` 0.85 (note the same key across ALL courses is green only 27% of the time — exactly why the tier is course-scoped), plain sprouts salads are green in 8 of 11, and the `crush` family is unanimously orange over two rows against `MIN_TOKEN_ROWS` 6. Keyed by course as well as name, because each verdict is argued from the dish's own course |
| `ikigai_masala-main/scripts/chennai_client_pools.py` | **Chennai.** The five holes four new clients (TCL, Gartner, World Bank, ICON Chn) found in the city list — four of them silent, one not: TCL went straight to INFEASIBLE on `welcome_drink (0 distinct item(s) for 5 day-slot(s))`. (1) **Kootu is the dal, not the gravy.** Three clients state "in dal need to give only Kootu item" in the same words, and all eight Chennai kootus were `course_type = veg_gravy`, so the dal pool held zero and the rule was unsatisfiable. Every one of the four sample weeks prints the kootu as its own row beside the gravies, never as one — World Bank has "Veg Gravy", "Kara kuzhambhu" and "Kootu" as three rows; ICON's is labelled "Kootu or Poriyal", pairing it with the dry. Re-filed to `dal`, `sub_category = kootu` preserved so `kootu_twice_weekly` still selects them. A deliberate divergence from Bangalore, which files 43 kootus as `veg_gravy` and two as `dal`. (2) Eight more kootus from Bangalore, eight vegetables Chennai's own do not cover (it had three ash-gourd ones). (3) **28 welcome drinks** where the city had NONE — not one row, `is_welcome_drink` zero across the list, which is why `chennai.json` documents Bangalore's drink rules as inert here; ten are buttermilks including `sambaram` and `tadka_neer_mor`, the dishes TCL's own grid names as SAMBARAM and NEER MOORU. `welcome_drink` is added to `ontology_categories.json`, so an empty pool now fails at build time naming the slot. (4) **`boiled_egg` and `bone_salna`**, named daily by two clients and absent from every city list — real rows rather than stamped text, so the pins narrow a cell and the dishes stay visible to every other rule. `bone_salna` gets `is_nonveg_gravy` but deliberately no regional chicken flag, or it would satisfy the "and a chicken gravy" component that exists to put a second dish on the plate. (5) Eight veg biryanis (TCL serves one daily against ~15 weekday services in a cooldown window) and six liquid sweets. (6) **Kuzhambu is the salad, not the gravy** — the client's own categorisation ("kuzhambus are the dish which category should be in salad not veg gravy"), and the same shape as (1). TCL states "in salad need to give only KUZHAMBU item" and its sample week proves the two are DIFFERENT rows rather than one mislabelled: Sunday's stated menu lists "veg gravy" and "salad" separately and the grid serves VEG KURMA beside KARA KUZHAMBU. Twelve rows re-filed `veg_gravy` → `salad` (veg_gravy 53 → 41, salad 35 → 47), `is_salad` set to follow. Deliberately NOT moved: the four non-veg kuzhambus, `kolambu_sadam`/`vatha_kuzhambu_rice` and `mor_kolambu_vada` — there the word names the gravy the dish is built from, not its own course. **The pool is city-wide and only TCL asked**, so the re-file alone put a sour gravy in five other counters' salad rows (Gartner, ToastTab CHN, Tekion CHN and two RNTBCI counters, 1-3 days of 5 apiece). Scoped by a PAIR: `chennai.json` carries `salad_is_not_a_kuzhambu` (`max: 0` @ salad) as the city default and TCL disables it. Neither half works alone — without the default the re-file leaks, without the disable TCL's own rule is unsatisfiable. Safe as a hard ban because 35 of the 47 salads are not kuzhambus. Idempotent; `test_chennai_client_pools.py` |
| `ikigai_masala-main/scripts/chennai_cuisine_corrections.py` | **Chennai only.** The same defect `ncr_cuisine_corrections.py` fixed for NCR: 31 plainly-Indian dishes in cuisine-main slots tagged `cuisine_family = continental`, and `_exclude_offtheme_cuisines` drops a continental dish on every non-continental day — **no Chennai client themes any day continental**, so all 31 were unservable. Found by a rule RELAXING, not failing: World Bank asks for a chicken gravy daily and by week three of a saved run served one on 3 days of 5, filling the gaps with an egg dosa. Chennai looked like it had 25 chicken gravies; it had 13. Every corrected row **contradicts itself**, which is what makes it a data fix rather than a judgement — the retag follows the `sub_category` (`chicken_north_*`/`chicken_tandoor` → north, `chicken_south_coastal` → south, `mushroom_stir_in_pepper` → south), except `pakora_/_bajji` (17 rows) where it names no region and the dish names do: vada, bonda, masala vada and the `65` family are Tamil tiffin, and `starter` is NOT in chennai.json's `exempt_slots` so the tag decides which themed days can serve them. **Left continental on purpose**: the four pasta rows and `veg_roll` (a cutlet — the master files 12 of 13 continental). A row whose sub_category names no region is reported, never guessed. NOT done for **Bangalore, which has 116 of the same self-contradicting rows** (53 `chicken_north_masala` + 52 `pakora_/_bajji`): its clients DO run continental days, so correcting them moves dishes off those menus as well as onto the others — a menu change to review rather than a dead-row cleanup, recorded in `docs/pending_config_changes.md`. Idempotent; `tests/cities/test_chennai_cuisine.py` |
| `ikigai_masala-main/scripts/definitional_flags.py` | **all cities.** Two more flags made true in **both directions** from the dish NAME inside a COURSE — the pattern `seafood_taxonomy.py` established for `is_fish_dish` and `bread_form_flags.py` for `is_plain_phulka_chapathi` ("a flag that is only ever set drifts"), now with a home of its own. **`is_liquid_dessert`**: NCR carried it on **55 rows that are not liquid** (petha, barfi, laddu, cake, brownie, gulab jamun) because `complete_ontology.py`'s token vote learned it from a dessert list that is mostly payasams, so "sweet" came to imply "liquid"; two more sat outside the dessert course entirely, `kheera_raita` and `kheera_raita_lemon_water`, the vote having read "kheera" as "kheer" — a client banning liquid sweets would have lost their cucumber raita. Nine genuinely liquid rows were unflagged. **`is_buttermilk`**: Bangalore flagged ten and left six unflagged including the plainest (`butter_milk`); Citrix, World Bank, ICON Chn and TCL all select on it. The clear reaches outside the course too, which is the half that catches the subtle ones. Also **re-files three buttermilk DRINKS out of `veg_gravy`** (`masala_chaas` in Bangalore and NCR, NCR's `butter_milk`) — `audit_course_types.py` missed them because `butter_milk` tokenises to `butter` + `milk`; `majjige_huli` and `mor_kuzhambu` are buttermilk CURRIES and stay gravies. Also two flags that are an INGREDIENT inside a course rather than a name family (`COURSE_INGREDIENT_FLAGS`): **`is_paneer_fry`** was **zero in every city** while Zscaler's `zscaler_paneer_fry_1` selected on it — `min` caps itself to what the pool can place, so the rule was silently inert rather than failing, and it is the only one of the 112 flag columns that is empty everywhere AND read by a shipped config. Its working twin **`is_paneer_gravy`** showed why: it was derived from `key_ingredient`, which in this ontology is the de-facto default for a CHINESE dish exactly as `baby_corn` is for a mixed salad, so Bangalore flagged `thai_green_curry`, `veg_in_hot_garlic_sauce` and five more as paneer gravies, and NCR flagged `chilli_chiken`, `tandoori_chcien`, `lemon_water`, `sauce` and a `lemon_mint_mojito` while leaving `butter_paneer_masala` and `matar_paneer` off. The definition therefore reads **`primary_protein` and the dish name, never `key_ingredient`** (`cottage_cheese` is in the name list, six dishes spelling paneer that way); across four cities that loses one real row and stops twenty wrong ones. All four flags are in `complete_ontology.py::OWNED_ELSEWHERE`, so the chain converges whichever order the two scripts run. Idempotent; `test_definitional_flags.py` |
| `ikigai_masala-main/scripts/deepen_thin_pools.py` | the three pools a client rule asked more of than the city list held — each found by a rule *relaxing* rather than failing, which is the failure mode worth chasing because nothing goes red. **Chennai chinese veg gravy 1 → 5** (one dish covered one Tuesday per cooldown window and none of the others), **Pune chaat starters 4 → 8** (Corning Chakan serves one every Thursday — four dishes was four Thursdays with nothing spare), **Pune leafy veg dry 5 → 9** (Corning wants two a week, needing ~8 distinct, and three of the five were fenugreek). The first two COPY from Bangalore (schema and attributes come free); the third builds NEW rows from a same-category Pune template with every `is_*` zeroed, because `palak/shepu/math/mulyachi chi bhaji` are Maharashtrian greens no city list carried and each takes a `key_ingredient` the ontology already uses — a new synonym would be invisible to every rule naming the old one. Idempotent; re-run after any re-import, since a raw re-import drops all twelve silently |
| `ikigai_masala-main/scripts/dump_client_rules_index.py` | regenerates `docs/client_rules_index.md` — **every per-client rule in one place**, one table per client: the rule name, a one-line summary of what it constrains, and the client's own sentence where the config's `_comment` records it. The rules live in 43 files and the client's words live inside comments, so answering "what is configured for Bakertilly?" or "who has a chapati-only bread slot?" meant grepping JSON. Generated rather than written because a hand-kept index is the third thing that can disagree with the configs — `--check` and `test_client_rules_index.py` fail if it is stale, and the same test asserts no rule type falls through to printing its own name (a summary that says `selector_frequency` tells the reader nothing) |
| `ikigai_masala-main/scripts/merge_enriched_ontology.py` | folds the client's `<city>_enriched_final.xlsx` workbooks (Bangalore/Chennai/NCR/Pune, in `source_workbooks/`) into the city lists. **A merge, not a replacement**: the uploads branch from a snapshot taken before the last three commits, so replacing would return NCR's vendor/calendar rows to its `veg_gravy` pool, bring the `raitha` duplicates back, revert twenty-one course fixes and restore 64 bare-integer `item_id`s. So the CURRENT workbook is the base and only VALUES cross over — `item_color` + `richness_score` overwrite (of 5,186 Bangalore dishes coloured in both files only six disagreed, and all six were rows we had just inferred; theirs is the better value each time), `primary_protein`/`cuisine_family`/`cuisine_family_region` fill blanks only, `spice_level` + `texture` are new columns (schema 134 → 136). Their literal `none` for "no protein focus" reads as BLANK on purpose — `_norm` folds it — because a blank already means that to every consumer while `none` would be a *matchable* value in a column `selector_frequency`, `ingredient_ban_rule` and `_nonveg_mask` all select on. Hyderabad has no enriched file (it did not exist yet) and takes its values from the merged Bangalore frame by dish name. One guard, `_refused()`: a non-veg `primary_protein` is accepted **only** on a `nonveg_main` row, because `_nonveg_mask` drops a non-veg row from every other slot and the dish becomes unservable — the enriched pass reads protein off the dish NAME and two rows prove a name can lie (`keema_veg_biryani` → `mutton`, the soya-keema trap; `red_velvet_pastry_egg_less` → `egg`). Their ROW edits are handled where they belong instead: junk they caught in `remove_generic_rows.py`, duplicate folds in `canonical_dish_spellings.py` (two verdicts reversed). Idempotent; `tests/data/test_enriched_merge.py` |
| `ikigai_masala-main/scripts/dump_chef_review.py` | regenerates `docs/chef_review_queue.csv` — the enriched workbooks' **own** open questions. `item_color` arrives 0% blank but **720 Bangalore and 354 NCR values are low-confidence**, and the files ship a `chef_review` sheet listing them; once merged they are indistinguishable from the 5,000 the client verified. A colour is not cosmetic (`_add_color_constraints` counts distinct colours per day and caps repeats), so this writes every flagged row with what the merge actually stored for it, lowest confidence first, and marks the ones no longer in their city's list. A report only — changes no data. `--check` fails if the committed CSV is stale |
| `ikigai_masala-main/scripts/dump_client_fixtures.py` | regenerates `tests/client_fixtures.py` from a `clients` export — **CSV or the `INSERT INTO … VALUES` SQL** the Supabase UI also offers (which is what the exports arrive as, and which cannot be split on commas: every JSONB column is a string literal full of them, with doubled apostrophes inside dish names). The fleet sweep is only as honest as that snapshot, and hand-editing 600 lines of literal is how it fell **13 clients behind** the live table — AT&T, Bakertilly, Citrix, Tekion CHN, ToastTab CHN, DXC, Corning Chakan and the six NCR sites were never swept, and nine clients that WERE swept had drifted (Citrix's whole theme map, Booking's slot counts, Clario's combo slot). Now 61 clients / 85 counters. A blank CSV cell stays `None` rather than becoming `[]` (`source_pools=None` means "the whole city list", which is not the same thing). `OVERRIDES` holds the deliberate deviations from live with their reasons — today just Quince's synthetic three-day week, the only coverage of the horizon filter — so a refresh cannot silently drop them |
| `ikigai_masala-main/scripts/protein_key_ingredients.py` | **all cities.** Standardises the `key_ingredient` vocabulary for protein sources, so the client's list (choley/rajma/kala chana/lobia/moong/masoor/urad/toor/chana dal/sprouts/soya/paneer/tofu/besan/peanut/khichdi/ghugni) can be selected on that one column. 651 Bangalore rows across rice/veg_gravy/veg_dry/salad/dal already carry one of 16 clean values; **7 name the same protein a second way** and were invisible to a `key_ingredient` selector — `channa_dal`+`gram_dal`→`chana_dal`, `cottage_cheese`→`paneer`, `soppu_moong`→`green_moong`, `allesande` (cowpea)→`black_eyed_pea`, `kofta_made_from_lentil`→`toor_dal`. A **vocabulary** fix only: nothing is reclassified and nothing changes category. `REGIONAL_PULSES` (horse_gram/avarekalu/chikkudikayi/broad_beans/cluster_beans) is deliberately NOT folded — real pulses, but their own ingredients rather than variant spellings, and renaming them to make a rule fire would misname the dish. Exports `PROTEIN_KEY_INGREDIENTS`, the canonical set the rule selects on. Idempotent; `test_protein_source_daily.py` |
| `ikigai_masala-main/scripts/add_ncr_north_rice.py` | **NCR only.** Copies 16 vegetarian North-Indian rices (rich/everyday pulaos + the khichdi family) from `bangalore.xlsx`. Every NCR counter themes most or all of the week `north`, so `rice` narrows to NCR's north rices — and 18 of the original 23 were mixed-veg pulao/biryani, a family `mixedveg_pulao_biryani_weekly` caps at **one day a week**, leaving 5 dishes for the other four days. Week 1 spent all five and from week 2 the cooldown had banned them, so the slot had nothing legal: that killed Carelon, Corning, SAEL, Siemens and Stryker NCR in week 2 and Airtel Noida in week 3. Usable north rice 5 → 21 against the ~19 a daily slot needs. `white_rice`/steamed variants are excluded by name (`NEVER`) — they belong to the `white_rice` CONST slot. Idempotent; `test_ncr_north_rice.py` |
| `ikigai_masala-main/scripts/ncr_south_bread.py` | **NCR only.** A counter may theme a weekday `south` (Junglee Games, Thursday) and the bread cuisine lock then narrows `bread` to `cuisine_family == south_indian` — of which NCR had **three**: `idli`, `idly`, `malabar_paratha`, the first two one dish spelled twice and **both `is_rice_bread`**. Once week 1 served `malabar_paratha` the cooldown left only rice-breads, coupling rule 38 (rice-bread ⇒ liquid rice) then demanded a liquid rice, and the same lock offers a south day 16 rices of which **none** is liquid (every khichdi is north) — INFEASIBLE with no starved slot to point at. Three parts, one concern: **retag** the idli/vada rows the mapping pipeline left with no `cuisine_family` (same fingerprint as `ncr_bread_misfiles.py`; `mini_idli`'s `sub_category=tandoor` corrected), **remove** the duplicate spellings (`idly`=`idli`; `idly_vada`/`idli_medu_vada`/`medu_vada_and_idli`=`idli_vada`), and **import** 12 south breads — 6 deliberately NOT rice-bread (kerala parotta, ragi/wheat/onion-tomato dosa, pesarattu, adai) so the slot is never *forced* into the coupling chain, plus 6 of the dosa/idiyappam family for variety. South bread 3 → 17, 7 of them non-rice-bread. Idempotent; `test_ncr_south_bread.py` |
| `ikigai_masala-main/scripts/ncr_fuzzy_unmerge.py` | **NCR only.** The raw NCR workbook fuzzy-matched dish names to the master at 0.82 similarity and overwrote the source name with the match (190 `ACCEPT_REVIEW` merges). Most are spelling variants and stay; **13 collapsed different dishes** (`aloo_matar`[peas]→`aloo_tamatar`[tomato], `punjabi_kadhi`[yogurt curry]→`punjabi_kadai`[wok], `bhuna_chicken`[Indian]→`hunan_chicken`[Chinese], …) — the client confirmed only spelling merges are allowed, so these are reverted. The merged rows kept the *source* attributes, so most fixes are a rename; 2 are collisions (a real dish also merged in) and are split into two rows. Idempotent; `test_ncr_fuzzy_unmerge.py` pins it stays reverted AND that a spelling merge is left untouched. See `docs/ncr_client_logic.md` |
| `ikigai_masala-main/scripts/city_list.py` | **the one place that knows which cities exist** — `CITIES`, derived from `data/raw/city_items/*.xlsx` (reference city first, then alphabetical, because several scripts learn from the deepest list first and `glob()` promises no order). Twelve all-cities correction scripts each carried their own `("bangalore", "pune", "chennai", "ncr")`, which was right for exactly as long as there were four cities: dropping in a fifth workbook would have left it carrying an OLDER set of corrections than every other city while loading, building pools and planning perfectly — nothing turns red. `tests/data/test_city_coverage.py` fails if a script goes back to a literal (with `INTENTIONAL_LITERALS` for the one that is right to be one — `expand_side_pools.SHARE_DONORS`, since lending a dish between cities is a menu decision and Quest's Telangana cooking does not belong in Pune's all-veg list) |
| `ikigai_masala-main/scripts/import_quest_hyderabad_menu.py` | Quest (Hyderabad)'s 41-day grid → **the new Hyderabad city list**. Two layouts OFFSET from each other and read apart by `_is_biryani_column` (row 2 blank ⇒ Wednesday's biryani menu, rows 5-13; otherwise the full Tue/Thu menu, rows 2-13) — reading every column on one map would file the Wednesday veg gravy as a dal. Four source-specific readings: **the two non-veg rows are dry and gravy in that order** (`style_by_label`, evidence no name heuristic has — a non-veg dish with no form flag is never placed by `nonveg_main_daily_pair`), and the **biryani row is a third style** because it prints `Andra Chicken Palvo` and `Hyderabadi Dum Pulao`, which the NAME rightly refuses to call biryanis; "Chef Choice Desserts" (14 of 41 days) is a placeholder, not a dish; the **fruit row is not a slot** and its values are serving counts ("Banana 2 nos") — `_COUNT_TAIL` also strips counts out of real dish names, or the same laddu written without one becomes a second row; and "Tossed Salad" printed in the YOGHURT row is re-filed by `_refile`. Idempotent; `tests/cities/test_hyderabad_ontology.py` + `test_hyderabad_plan.py` |
| `ikigai_masala-main/scripts/normalize_item_ids.py` | **all cities.** One key format: `item_id` is `MENU######` on every row of every city. Two allocators computed "one past the city's highest" with `pd.to_numeric`, which coerces `MENU004360` to NaN — the max of an all-NaN column is nothing, so they fell back to 1 and stamped **bare integers** onto 64 rows (56 Chennai via `chennai_client_pools.py`, 8 Pune via `deepen_thin_pools.py`). Nothing had broken, which is the point: the bad maximum is recomputed every run, so each future addition to those cities would restart at 1 and eventually collide with a row already numbered 1. Both allocators now parse the numeric part the way `expand_side_pools` always did; this repairs what they wrote. Hyderabad's 101 imported rows were renumbered off `HYD` for the same one-format reason — the prefix bought nothing, since `item_id` is unique only WITHIN a city and Chennai/NCR/Pune already overlap Bangalore's range. Idempotent; `tests/data/test_item_ids.py` |
| `ikigai_masala-main/scripts/normalize_city_ontology.py` | turns a raw city workbook into `data/raw/city_items/<city>.xlsx`: column set + order forced to the reference ontology's, `is_*` flags coerced to 0/1, text trimmed, `client` pool column set (default `common` — a city list tagging every row with one client name carries no per-client information and would leave a `source_pools: []` client with zero items; `--client-pool keep` opts out). Reports the categories the list does not cover. `--dry-run` writes nothing |
| `ikigai_masala-main/scripts/setup_all.sql` | **the only** schema script: idempotent, creates every table + applies the counter migration in one run. The `create_tables.sql` / `create_history_tables.sql` component files it superseded were removed, so there is one path to a correct schema rather than two |

---

## 7. Call graphs (typical flows)

### Generate menu
```
Streamlit app.py
  → MenuApiClient.plan()  [ui/api_client.py]
  → POST /api/v1/plan     [api/app.py]
    → @solver_gate                                      [api/concurrency.py]
    → ClientConfigLoader.get_client()                   [src/client/client_config.py]
    → ontology.repository.filtered_menu_data(city, pools)  [src/ontology/repository.py]
        (cached; ExcelReader → ColumnMapper → DataCleanser → PoolBuilder on a miss)
    → application.horizon / constant_items / history    [src/application/]
    → MenuRuleLoader.load_from_file()                   [src/menu_rules/menu_rule_loader.py]
    → HistoryManager (Supabase)                         [src/history/history_manager.py]
    → MenuSolver.solve()                                [src/solver/menu_solver.py]
        for each (day, slot): rule.pre_filter_pool()
        build CP-SAT model, add rule.apply() constraints
        optimize sum(get_objective_terms())
    → SolutionFormatter.to_dict()                       [src/solver/solution_formatter.py]
  ← JSON → Streamlit table
```

### Regenerate cells
```
POST /api/v1/regenerate  [api/app.py]
  → MenuRegenerator.regenerate(base_plan, replace_mask)  [src/solver/regenerator.py]
    lock untouched cells, re-run MenuSolver.solve()
    rank by similarity_score()
```

### Save to history
```
POST /api/v1/save  [api/app.py]
  → HistoryManager.save() → Supabase (menu_history, week_signatures)
```

### Edit client config
```
Streamlit customisation/* → PUT /api/v1/client-config/<name>  [api/app.py]
  → ClientConfigLoader.update_*() → Supabase
  (live reads elsewhere; no restart)
```

---

## 8. Tests (`ikigai_masala-main/tests/`)

**Layout** — 77 files in one flat directory made a subject hard to find (NCR
alone had 8 scattered across it). Grouped by what a change touches, so the tests
for one edit sit together:

| Directory | Holds |
|---|---|
| `tests/clients/` | per-client logic (DXC, NCR sites, Pune, Chennai, protein/veg-dry rules), the client-config layer, **and the config-validity guards** (`test_client_disable_targets` / `test_constant_pin_targets` — does a client file name real things) |
| `tests/cities/` | per-city ontology, ruleset and end-to-end plan (`ncr_*`, `pune_*`, `chennai_*`, `city_ontology`) |
| `tests/rules/` | the rule engine — every rule type, the loader, diagnostics, the solver |
| `tests/data/` | ontology corrections + the preprocessing pipeline |
| `tests/platform/` | API, concurrency, rate limit, metrics, logging, **architecture** |
| `tests/ui/` | formatters, planner view, API client |
| `tests/` (root) | fixtures (`conftest`, `fake_supabase`, `client_fixtures`) + the three genuinely fleet-wide suites: `test_all_clients_generate`, `test_integration_slow`, `test_p0_fixes` |

Each directory carries an `__init__.py` — `tests/` is a package, so its
subdirectories must be too or `from tests.fake_supabase import …` breaks.

| File | Target |
|---|---|
| `conftest.py` | fixtures (project_root, sample data path) |
| `test_api.py` | Flask endpoints |
| `test_solver.py` / n/a — see `test_rule_constraints.py` | solver + constraint integration |
| `test_menu_rules.py`, `test_rule_constraints.py` | rule behavior |
| `test_menu_rule_loader.py` | JSON deserialization |
| `test_pool_builder.py` | pool & slot expansion |
| `test_column_mapper.py` | alias/normalize/derived cols |
| `test_slot_composition.py` | theme-filter union + slot composition |
| `test_prefilter_integration.py` | multi-rule pre-filter chain |
| `test_history_manager.py` | cooldowns & signatures |
| `test_client_config.py` | Supabase-backed config |
| `test_formatters.py`, `test_helpers.py`, `test_solution_formatter.py` | UI/utility layers |
| `test_city_ontology.py` | per-city ontology resolution + caching, `build_pools(required_slots=…)`, and the shipped Pune file (columns, declared categories, all-veg, `common`-only pools) |
| `test_normalize_city_ontology.py` | the normaliser's pure `normalize()` + a check that re-running it on the committed Pune workbook is a no-op (so nobody wonders whether it was hand-edited) |
| `test_pune_rules.py` | the Pune ruleset (every rule valid, named to an R-number, and actually matching Pune items — inert ones are listed explicitly) + the two new rule types |
| `test_pune_plan.py` | Pune end-to-end: every dish comes from the Pune list, the menu obeys R1/R2/R3/R12/R13/R19/R25/R28/R33/R43/R51, and week 2 still has bread after week 1 is in history (R36's payoff) |
| `test_pune_client_logic.py` | Amadeus Pune against the client's own sample week (`docs/pune_client_logic.md`): which slots run on which weekday, chapati/buttermilk daily, one paneer and one soya a week, Sunday's biryani-only menu — plus the four engine fixes that week needed |
| `test_architecture.py` | **executable layering constraints.** Walks the full AST of every `src/` module — including function-local imports, where all three historical violations hid — and asserts nothing in `src/` imports `api`/`ui`/`customisation`/`streamlit`/`flask`; separately imports `src/` with `api` and `streamlit` blocked from `sys.meta_path`. Also pins that no test file names the retired ontology cache globals (setting them silently does nothing and loses isolation), that `reset_caches()` empties everything, and that cities sharing a workbook share one cache entry |
| `test_course_type_audit.py` | that no dish sits in a category belonging to something else, per city — zero unadjudicated name/`course_type` mismatches, plus a planted misfile and an is-it-matching-anything check so the guard cannot pass vacuously, plus that no `ADJUDICATED` entry names a row since renamed away. Also pins `tomato_thokku` → `veg_gravy` (D2) |
| `test_generic_rows.py` | the 11 category-named rows (`scripts/remove_generic_rows.py`) stay removed, removal matches whole names not substrings, and no affected slot is starved below a 7-day week |
| `test_dessert_cuisine.py` | the dessert `cuisine_family` retag (`scripts/dessert_cuisine_corrections.py`): the payasam/kesari/mysore-pak families are `south_indian`, western bakery is `continental`, `milk_cake` stays Indian, and East/West sweets stay north by design |
| `test_seafood_taxonomy.py` | the seafood branch of the ontology: the two flag columns exist in every city workbook and in one shared order, Chennai's seafood rows carry no chicken sub-category/flag/key_ingredient, `is_fish_dish` ⊆ `is_seafood` **and equals exactly the `FISH_PROTEINS` rows** (prawn is seafood, not fish), the cuisine fixes match the master's own `chicken_65`, a rule actually reads `is_seafood`, and re-running the script is a no-op. The counts are derived, not pinned — "exactly 8 fish rows" broke the moment the menu bank import legitimately grew the list |
| `test_chennai_rules.py` | the Chennai ruleset + ontology (every rule valid and matching real items, declared categories non-empty, the staple slots that would otherwise starve, and the bread-exemption fix in both directions — Chennai unfiltered, Bangalore still narrowed) |
| `test_chennai_client_logic.py` | Toast Tab CHN against its sample week: exactly one rice a day (white on south days, flavoured on north/biryani), the sour component following the rice, a wheat flatbread every day and never a dosai, non-veg daily, zero diagnostics |
| `test_ncr_fuzzy_unmerge.py` | the NCR fuzzy-merge reversal (`scripts/ncr_fuzzy_unmerge.py`): the 13 wrong-merge names stay reverted, every restored dish is present, the 2 collision splits kept both dishes, `bhuna_chicken`/`paneer_achari` attribute fixes hold, a pure spelling merge (`ajwain`→`ajawin`) is left untouched, and re-running is a no-op with no dup ids/names |
| `tests/cities/test_hyderabad_ontology.py` | the seeded city list: it no longer falls back to Bangalore, is the master 134-col schema, is deliberately **absent** from `ontology_categories.json` (a Bangalore superset covers every mandatory slot, so the undeclared default is the STRICTER check), is a strict SUPERSET of its seed (`expand_side_pools` used to drop four — its un-share pass read every blocklisted name in a non-master city as a bad cross-city share, true of a city built from its own list and false of one seeded from the master), the 101 additions all carry `quest`, the placeholder / fruit row / serving counts / daily staples were all kept out, every new non-veg row has a form flag (incl. the two pulaos the biryani ROW settled and the name could not), the pools clear the cooldown floor, and the re-import is a no-op |
| `tests/cities/test_hyderabad_plan.py` | Hyderabad end-to-end: clean diagnostics, a real 5-day solve drawn entirely from `hyderabad.xlsx`, non-veg tagged, the ruleset still identical to Bangalore's — and the pool assertion that matters, that a client with `source_pools = []` reaches the whole 6,260-row list and can be served Quest's dishes rather than the 960 `common` rows |
| `tests/data/test_cuisine_family.py` | that a themed day can see its pool: the four settled cities stay complete, NCR's cuisine-main blanks stay under 100, a north day can fill each slot, and only the known vocabulary is used. Also what the vote REFUSES — that it can propose only the two Indian regions, that a theme-exclusive row is never *written* a region (asserted by re-running the pass, since some such rows arrived with one and the pass is monotone), and that the thresholds are still the measured ones. Plus the two tiers that beat the vote, each with its counter-check: the north-chicken rows got their region, `mixed_veg_curry` is >90% north within NCR **and** still below the agreement threshold across the corpus — without that second half the city-scoped tier could stop being the reason those rows are filled and nothing would notice |
| `tests/data/test_item_ids.py` | that `item_id` is one format everywhere and unique within each city, that uniqueness is only promised within a city (asserted positively, so nobody builds on the opposite — Bangalore and Chennai ranges genuinely overlap), that all three allocators agree on the highest existing id, and — pinned directly — that `pd.to_numeric` over a prefixed id yields nothing, which is the specific mistake that caused it. Plus the repair's invariants: it renumbers past the highest number whatever prefix carries it, so a new id can never land on an existing row |
| `tests/data/test_city_coverage.py` | that every all-cities correction script reaches every workbook. Twelve scripts each carried their own 4-city tuple; the failure mode of a fifth city is silent (it simply keeps an older set of corrections while loading, building and planning fine). Asserts the derived list matches the directory, that each script imports it, and — by reading the AST — that none has gone back to a literal, with `INTENTIONAL_LITERALS` for the one that is right to be one |
| `test_ncr_plan.py` | NCR end-to-end: the city ontology + Bangalore-inherited ruleset, a real solve drawing every dish from the NCR list (all 8 pools, since NCR has no `common`), non-veg tagged, zero blocking diagnostics |
| `test_ncr_cuisine.py` | the NCR savoury cuisine retag (`scripts/ncr_cuisine_corrections.py`): no `continental` left in nonveg_main/starter, the North Indian chicken + Indian starters are `north_indian`, genuinely-continental veg_dry/soup untouched, re-run is a no-op |
| `test_ncr_sambar.py` | the 10 sambar added to NCR (`scripts/add_ncr_sambar.py`): all present + filed as sambar, ids/names unique, schema unchanged, present in the built pool, re-run is a no-op |
| `test_ncr_bread_misfiles.py` | the curries-filed-as-bread cleanup (`scripts/ncr_bread_misfiles.py`): none of the twelve is in the bread pool, each re-filed dish landed in its category, `paneer_jaipuri` is a paneer `veg_gravy` with no bread flags, the two removals are gone, ids/names unique, re-run is a no-op |
| `test_protein_source_daily.py` | the protein-a-day rule (Tekion/Stryker): the 7 variant spellings are folded and their dishes now carry a listed protein, every fold target is on the client's list, the regional pulses are left alone and opted out, the fold is idempotent, all five slots can supply a protein, `daily_min` validates alone and rejects a negative, `base_slot` accepts a list AND a string (single-slot configs unchanged), and the wired rule loads for both clients matching **only** listed key_ingredients |
| `test_indian_veg_dry.py` | `indian_veg_dry_themes` (Tekion/Stryker/Stripe's Chinese day): an unconfigured rule is unchanged, a listed theme narrows `veg_dry` to north+south (**both** kept so the solver picks per day), continental is still excluded, other slots and other theme days are untouched, an all-Chinese pool falls back instead of emptying the slot, and `_project_filter_size` models the same branch so diagnose and the solve agree |
| `test_ncr_north_rice.py` | the 16 north rices added to NCR (`scripts/add_ncr_north_rice.py`): all present + filed as rice, north + vegetarian, **outside** the weekly-capped mixed-veg pulao/biryani family, ≥19 uncapped north rices so a daily slot outlasts the cooldown, no `white_rice` variant imported, re-run is a no-op |
| `test_ncr_south_bread.py` | NCR's south bread pool (`scripts/ncr_south_bread.py`): the 12 imports present + south, **≥5 of the south breads are not rice-bread** (the constraint that mattered — otherwise the coupling chain is forced), ≥12 south breads so a weekly south day outlasts the cooldown, the idli/vada rows retagged south + flagged `is_rice_bread` (and `mini_idli` no longer `tandoor`), the four duplicate spellings gone with the kept name present, re-run is a no-op. Also the two cadence rules: `ncr_south_bread_cadence` valid + window longer than a week, matching every south bread and only breads; `ncr_south_bread_weekly_max` present as the within-plan half; **banning the whole family still leaves the south day a bread** (the fallback that stops the cadence being a weekly guarantee of failure); and NCR is in `FULL_POOL_CITIES` so naming one site pool yields the same item count as the full list |
| `test_corning_pune_import.py` | the Corning Chakan Pune import: only the plated lunch/dinner rows are read, no bare salad-bar vegetable became a dish, its chaats are starters, each of the three two-slot rows splits by name, the chutney row's bare ingredients gained the category word (and `schezwan` folded onto the row Pune already had), the unlabelled festival block is read by name and never by position, the word-order duplicates collapsed to one row each, `mix_veg` cannot reach the bread slot, Pune stays all-veg with every row `common`, `veg_kheema`/`soya_kheema` did not become chicken, the pools it was meant to deepen grew, and the re-import is a no-op |
| `test_marathi_ingredients.py` | the Marathi ingredient vocabulary: every emitted value already exists in the ontology, the short ontology spelling wins, `kala chana` uses the on-list `chickpea`, `chana_dal` beats a bare `chana`, the course picks whether the lentil or the vegetable leads, the `flaxiseed` typo is folded, blanks only are filled, and a second pass fills nothing |
| `test_complete_ontology.py` | the ontology completion pass (`scripts/complete_ontology.py`): every course mirror still holds in **both** directions (no row of the course missing the flag, no row outside it carrying one — the re-file residue check), column mirrors agree with their column, the recorded coverage is not stale, no row holds two flags a course never combines, exclusive pairs are stored sorted (they were stored in column order and looked up sorted, so every pair was a silent miss), conflict resolution prefers the name over an attribute and a later word over an earlier one, the premium predicate matches the client's rule including `baby_corn` by phrase, the mixed-vegetable catch-all was NOT applied, a scoped column is never filled outside its courses, stopwords never justify a fill, and a second pass fills nothing |
| `test_drop_dead_columns.py` | the dead-column removal: `universe` gone from all four, the schema is 134 and identical everywhere, no rows lost, a column holding data in ANY city is refused, an empty one is dropped from every city, `--dry-run` writes nothing, and the re-run is a no-op |
| `test_expand_side_pools.py` | the side-pool expansion (`scripts/expand_side_pools.py`): +7 per category per city, curated flavoured chapatis present everywhere, all additions veg (Pune), pool token matches the city convention, cloned desserts carry clean flags (no inherited `is_fried`/`is_sugar_syrup`), schema + id/name uniqueness, and the no-op re-run |
| `test_selector_history_window.py` | the cross-week cadence rule: `selector_banned_by_date` (recent occurrence bans the family, outside-window / no-history / unrelated-history ban nothing), the rule's validation + selector→item resolution, loader registration, and the `_build_history_context` merge into `banned_by_date` (fish 4 days ago banned; 33 days ago not) |
| `test_ncr_client_logic.py` | the 5 NCR sites' logics against real solves: Stryker paneer-2×/egg-1×, Siemens 2-chicken-daily, Airtel non-veg-Wed/Fri-only, Sinch chicken-MWF/egg-Tue-Thu, Junglee chicken-4×/egg-1×/paneer-1× |
| `test_dxc_client_logic.py` | DXC (Bangalore launch site, 2 counters) against real solves: biryani≥3/pulao≥1/no-south flavoured rice, plain-chapati daily (+ week-2 staple survival), raita Mon/Tue/Thu/Fri + Wed curd, and the **cross-counter shared-categories sync** (note 22) — the two counters serve identical bread/rice/sambar/rasam/curd/dessert/white_rice per day while `nonveg_main` stays independent |
| `test_pune_integration.py` | Pune through the whole stack: every endpoint, `/save` → `/plan` again (the cooldown reading history the API wrote), `/regenerate` keeping restricted slots absent, and the Streamlit layer (labels, red-dish tagging, Excel) |
| `client_fixtures.py` | snapshot of the live `clients` table — **all 43 clients / 57 counters** (`CLIENTS`) + `APP_SETTINGS`. Regenerate when the live table changes shape |
| `test_all_clients_generate.py` | **`@slow`** whole-fleet sweep: every counter in `client_fixtures.CLIENTS` must return a plan, plus named cases for working_days, pinned constants, day restrictions and starved slots |
| `test_p0_fixes.py` | fast stand-ins for the sweep's assertions (no solve, no Excel) so regressions still fail on a PR |
| `test_freshness_variety.py` | the soft freshness objective (note 24): `days_since_last_served` (counts from `as_of`, ignores same-day/future, excludes staples), `_freshness_bonus` bounds (never-served → cap, capped, 0 without a map, below LOW / above the tie-break), and two real solves — the least-recently-served candidate wins an unconstrained cell, and an all-recently-served map never forces INFEASIBLE |
| `test_same_day_exclusion.py` | `same_day_exclusion` both scopes: day (two families never share a day) and **week** (`scope: week` — the pair never shares a plan, "biryani not the same week as fish"), the forced-both relaxation, and the both-sides dish belonging to the selector |
| `test_shared_categories.py` | the config layer for `shared_categories`: `get/set_client_shared_categories`, `_normalize_shared_categories_value` (keeps known base/const slots), the `create_client`/`update_client_atomic` round-trips, and `GET /client-config`'s DB-value-else-file-value fallback |
| `test_pool_token_labels.py` | `pool_token_client_labels` maps ontology pool tokens to app client names (word-subset match scoped to the city, unmatched tokens title-cased) so the Item Pools multiselect reads in client names |
| `test_fill_item_colours.py` | the colour tiers in order of evidence (a name that carries a colour word is never overruled by a blunter signal), that every learned rule meets its stated threshold, that a further pass fills nothing (the fixed point), that the greedy question-grouping is deterministic across processes, and that the committed report matches the workbooks — a stale one asks the client for dishes that have since been coloured |
| `test_chennai_cuisine.py` | the Chennai continental retag: no chicken curry and no pakora/vada is left continental, the named rows landed where their `sub_category` says, the pasta and the cutlet are deliberately untouched, salads and soups are not touched at all (they are not cuisine-main slots), ≥20 servable chicken gravies so World Bank's daily rule survives the cooldown, the script refuses to guess an unmapped sub_category, a planted mislabel is corrected, and the no-op re-run |
| `test_chennai_client_pools.py` | the five Chennai holes stay closed: every kootu is filed `dal` with `sub_category` intact and across ≥6 distinct vegetables (depth that is more ash gourd is not depth), the veg_gravy pool survived losing them, 28 welcome drinks with ≥10 buttermilks AND ≥15 non-buttermilks (TCL caps buttermilk at twice a week, so a pool of buttermilks alone would make the cap unsatisfiable rather than tight), `welcome_drink` declared in `ontology_categories.json`, `boiled_egg` dry and `bone_salna` a gravy but NOT a chicken gravy, neither inheriting its template's flags, ≥20 veg biryanis and ≥15 liquid sweets, every import tagged `common`, schema 134, the no-op re-run, and the kuzhambu re-file (every named one is a `salad` carrying `is_salad`; the non-veg/rice/starter kuzhambus did NOT move; Bangalore untouched). The veg_gravy floor is stated as what a DAILY gravy needs under the 20-day cooldown rather than as a snapshot — that pool has now lost dishes twice |
| `test_constant_pin_targets.py` | that a `constant_items` pin naming a dish its city HAS is not a typo of one. Note 9's two readings — narrow the cell, or stamp the text — are both wanted, so the check is the one question independent of which branch honours the pin: does the name match anything? A typo is indistinguishable from a deliberate off-ontology stamp and fails in the expensive direction, the menu still reading correctly while the dish stops being a candidate. Two were live: Ather's `mix veg salad` (the dish is `mixed_veg_salad`) and Booking.com's `veg kati roll` (`veg_kathi_roll`, from Booking's own import). `OFF_ONTOLOGY_ON_PURPOSE` lists the five deliberate ones with reasons, and a separate test fails if the ontology later gains one of those dishes — note 9 says that switches the pin over with no config change |
| `test_fixed_items_without_a_slot.py` | that a pin may be a fixed item without the slot being a configured category, and that the guard still fires where it should: a sole-counter or counter-scoped pin survives, a client-level pin on a multi-counter client is still dropped, the `curd`/`curd_side` sibling exception is unchanged, an unknown slot is still refused, and — through the real `_rules_and_skip_for_client` — an unserved slot is STAMPED rather than forced while a served one is still forced |
| `test_multi_counter_throughput.py` | that one Generate click on a multi-counter client cannot exhaust the limiter. The planner solves each counter with its own `/plan`, so a click on a six-counter site is six requests against a bucket that held ten — the counters past the cutoff answered 429 and their tabs had no menu. Sized from `MAX_COUNTERS` so the ceiling cannot drift, still throttles a loop, and the client now honours a 429's `Retry-After` (capped) instead of sleeping a 0.2-0.7s jitter against a bucket refilling at 0.3 tokens a second, where the retry could never succeed |
| `test_client_disable_targets.py` | that a client's `disable` names a rule its own **city** actually has, and that every `use.ref` names a component `rule_library.json` defines. Both are dropped silently when they match nothing — the same shape as the `clients.name` mismatch in note 9, and it had already happened: `Tekion CHN` was written by re-keying Tekion BLR's block and carried BLR's `disable: ["deep_fried_coupling"]` across, a **Bangalore** rule that standalone `chennai.json` has no equivalent of. Inert in the harmless direction that time; the guard is so the next one is not. Also pins that every client-file key matches a real client row. **And that every rule in every client file actually builds and validates** (`TestEveryClientRuleActuallyBuilds`) — `load_for_client` logs a warning and moves on when `_create_rule` or `validate_config()` fails, so an unknown `type` or a misspelled field costs the client that rule while `/plan` still answers 200 and `/diagnose` still reports clean. The per-city rulesets had this check (`test_pune_rules`, `test_chennai_rules`); the 36 per-client files had none, which is where the risk is — a city ruleset is written once and reviewed, a client block is written per site and copied between them. Checked in both directions, since a rule the loader never returns would pass a validity check by being absent |
| `test_definitional_flags.py` | `is_liquid_dessert` and `is_buttermilk` in both directions, per city: no dessert outside the definition carries the flag and no liquid one is left without it, the buttermilk flag equals the predicate row for row, **neither flag appears outside its course** (the half that caught `kheera_raita`), the three buttermilk drinks re-filed out of `veg_gravy` while `majjige_huli` stays one, a planted false positive is cleared, and `complete_ontology.py::OWNED_ELSEWHERE` lists them so the chain converges. Also the two paneer flags: the predicate reads `primary_protein` and the name and **refuses `key_ingredient`** (pinned against `thai_green_curry`, `bok_choy`, `chilli_gobi`), each city's workbook agrees with the definition row for row, `is_paneer_fry` is no longer empty where a rule reads it, and — the general form — **no `is_*` column any shipped config selects on is empty in every city** |
| `test_chennai_rule_grammar.py` | the five engine gaps the new clients walked into: `slot_indices` skipping ONE expansion (and never the bare base slot, which would take the whole family with it), `forbidden_weekdays` parsing and standing alone as a constraint, `all_of` requiring every part and nesting with `any_of`, a dish pinned on 2+ days being declared repeatable in the shape `unique_items` actually reads (a parsed matcher tuple, not a selector dict) while a ONE-day pin is not, and an out-of-range constant being dropped rather than clamped onto a cell another pin already holds |
| `test_chennai_new_clients.py` | **`@slow`** — TCL, Gartner, World Bank and ICON Chn against real solves of all eight counters: TCL's seven-day week with its two reduced weekend menus, one-rice Saturday, kootu dal, chapati bread, biryani + south rice pair, egg on Mon/Wed/Fri, three liquid sweets, two buttermilk days and no premium veg at the weekend; Gartner's three complementary rice restrictions and Wednesday fish; World Bank's four daily non-veg dishes with the three pins repeating and the fourth cell still rotating; ICON's Mon/Wed/Fri premium station, the dal/veg-dry alternation on two counters, matching egg-gravy weekdays on Economy and Roti, and the Rice Combo's exclusion from the shared sync |
| `test_deepen_thin_pools.py` | the three deepened pools stay deep: the four Chennai chinese gravies present + veg, ≥6 Pune chaat starters (and NOT `papdi_chaat`, which duplicates Pune's own `papadi_chaat` under a second spelling), ≥8 Pune leafy dries across ≥4 DISTINCT greens (depth that is three more fenugreek dishes is not depth), every new `key_ingredient` one another city already uses, no inherited template flags, schema 134, and the no-op re-run |
| `test_nonveg_structural_flags.py` | dry vs gravy for every composed row. Now asserts **zero** unresolved per city (the client adjudicated all 51 — the test used to assert the opposite, which was right while the verdicts were theirs to give), that the mechanism still refuses to guess for an un-adjudicated dish, the four Bangalore rows that are genuinely not sauced against their sauced near-namesakes (`egg_vepudu` fry vs `egg_pulusu` stew), and that Chennai's eleven came out unanimously dry — a mixed result there would have meant the reading was arbitrary |
| `tests/rules/test_per_week_caps.py` | note 26: `max_per_week` stands alone, rejects a negative, may pair with `max` for a plan-wide ceiling but not with `exact`, and is rejected when LOOSER than `max` (silently taking the tighter one would hide a config mistake); `max` still means the horizon. ISO-week counts over seven horizons, plus an explicit test that a mid-week start MAY allow two in seven days — the looser reading of "a week", on the record rather than as a surprise. Then the migration: no `*weekly*`-named `selector_frequency` rule anywhere in `data/configs/` still uses `max`, every city's biryani cap is per-week, and — read from the SOURCE, because the failure mode is the two halves disagreeing and no single-horizon test can see it — both rule modules mention `isocalendar()` and `_forced_dates` |
| `tests/rules/test_rule_instance_isolation.py` | note 27: a client with no overrides gets FRESH rule instances, two clients share none, every rule survives the rebuild (a rebuild that dropped one would trade a rare 500 for a permanently ignored constraint), and a config-less stub is passed through. Then the damage itself, measured rather than assumed: one rule, two interleaved model builds, and the first model's objective solved — MODEL_INVALID when the neighbour is bigger, **OPTIMAL against the wrong variables** when it is the same size or smaller. The fixture is deliberately a STARVED slot, since `apply()` only records variables when uniqueness has to be relaxed and a comfortable pool would reproduce nothing |
| `tests/platform/test_non_working_days_are_blank.py` | note 29: 5 asked for is 5 columns back whatever the client's week is, the blank ones are exactly the unserved weekdays, every served day still has a menu, an unrestricted client is unchanged, and 10 days is two Mon-Fri weeks (two blank Fridays, eight menus). Then the marking — `is_working_day: False` and no theme on an off day, its theme intact on a served one — and the UI: a dimmed "Not served" header, no such header for an unrestricted client, the Excel export keeping the column, and `off_days_from_solution` defaulting to empty for a payload without the new flag. Plus that the solve is untouched: nothing planned on an unserved day, and a `max_per_week` rule still counts two ISO weeks over ten days |
| `tests/platform/test_save_canonicalises.py` | note 28: a display name is stored as the ontology spells it, the solver's own spelling is untouched, a colour suffix is still stripped, and a clean save's body is byte-for-byte what it was. Then the consequence, asserted through `banned_items_by_date` rather than through the stored string — the point is that the cooldown FIRES, not that a cell changed. Then the reporting half: an unresolvable name is named in `unknown_items` but still saved, a declared off-ontology pin is not reported (Plan View's `boiled egg`, which Bangalore genuinely lacks), the `_comment` prose beside a pin is not folded into the allow-list, both save bodies canonicalise, and an ontology read that raises still lets the save through |
| `tests/data/test_enriched_merge.py` | the enriched merge: `_norm` reads `none` as blank so no matchable value enters `primary_protein`, the overwrite columns took the client's value, the fill-blanks columns did not overwrite an adjudicated one, the two new columns are present in every city at 136, `_refused` blocks a non-veg protein outside `nonveg_main` (with the two real rows pinned, and a planted one so it cannot pass vacuously), Hyderabad's values match Bangalore's by dish name, and a second merge changes nothing |
| `test_client_rules_index.py` | that `docs/client_rules_index.md` still matches the configs (recomputed, not spot-checked), that every configured client has a section and the header's count is current, and that **no rule type falls through to printing its own name** — a summary reading `selector_frequency` tells the reader nothing, so each type a client file actually uses must render a sentence |
| `test_client_update_logic.py` | **`@slow`** — AT&T, Bakertilly, Citrix and Tekion CHN (the `client_update.xlsx` sheet) against real solves: chapati-only bread, the paneer/soya-or-kofta/pulao counts, Bakertilly's biryani-day stand-down plus its biryani-day-only chicken dry, Citrix's buttermilk-every-day and Mon/Wed-gravy Tue-egg Fri-biryani schedule, the same-region-per-day and alternate-region-across-days pair, and Tekion CHN drawing only from the Chennai list |
| `test_bread_form_flags.py` | `is_plain_phulka_chapathi` means one thing in both directions: the predicate's edge cases (a millet or tandoor roti is not a chapati, a `*_chapati_dosa` combination row is not either), no flagged row outside `course_type == bread`, no chapati-named bread left unflagged, the 28 named offenders gone, a pool deep enough for a daily slot, and a planted wrong/missing flag corrected so the guard cannot pass vacuously |

Run: `pytest` from `ikigai_masala-main/`. `pytest.ini` sets `-m "not slow"`, so the
57-counter sweep and the constraint-integration solves are **deselected by
default** — run `pytest -m slow` (CI does this on push-to-main and on dispatch)
to exercise them.

---

## 9. Non-obvious design notes

1. **Cell-based CP-SAT**: one bool var per (day, slot, candidate) — enables per-item penalties and similarity-driven regeneration.
2. **Two-phase rules**: pre-filter eliminates items before the model is built (fast), then `apply()` adds CP-SAT constraints. When writing a new rule, decide which phase fits.
3. **No config cache**: `ClientConfigLoader` reads Supabase on every call — live edits, no restart. Don't add caching without thinking through invalidation.
4. **Dynamic worker allocation** in `api/concurrency.py`: 1 active solve → 9 workers; 2 active → 5 each. Tuned for ~1 GB RAM.
5. **Theme dispatch**: global weekday→theme map, per-client overridable via each counter's `theme_map` in `clients.counters`. Themes: mix/chinese/biryani/south/north/**continental** + **`chinese_continental`** (meta-theme resolved per ISO-week parity in `weekday_type_for_config` → even=chinese, odd=continental). Solver honors it via `theme_*` rules.
10. **Weekend service**: `clients.serve_weekends` — when set, `_weekdays_from` stops skipping Sat/Sun so plans cover them.
12. **Per-client item cooldown**: `clients.item_cooldown_days` (None=default 20). `api._apply_item_cooldown_override` rebuilds a fresh `item_cooldown` rule (never mutates the cached shared list) and the value is passed to `banned_items_by_date`; `_effective_history_window` widens the history query to match.
25. **No-repeat is HARD; the only escape is the declared-slot list, never a silent relaxation.** `ItemCooldownMenuRule.pre_filter_pool` removes every cooled-down dish outright and never "borrows back" a recent dish to fill a gap — if that empties a slot, `diagnose()` raises an **ERROR** and the fix is *more dishes in that category for that city*, not a quietly repeated menu. Repetition is permitted only where it is declared: `REPEATABLE_SLOTS` (`curd` — same dish daily), `COOLDOWN_EXEMPT_SLOTS` (`curd_side`/`curd_rice`/`soup`/`healthy_rice` — cycle through every distinct dish, then repeat), and per-dish staple declarations (`repeatable_items`, e.g. Pune's plain chapati). Deliberately NOT exempt: `rasam` and `sambar` — they are core dishes where a kitchen carries real variety, so they are fixed with data, not with a relaxation. A graceful-degradation variant (restore the least-recently-served dish rather than starve) was built and then reverted for exactly this reason: it made every slot silently repeatable, which is the opposite of the product rule. Sizing follows from the window: a strict count-1 slot needs roughly one distinct dish per working day in the cooldown window (~15 for the 20-day default) plus the week being planned.
17. **One item ontology per city, selected from `clients.city`** — the same shape the rules already had. `src/ontology/paths.py::city_excel_path(city)` (re-exported from `api/config.py`) resolves `data/raw/city_items/<slug>.xlsx`, falling back to `DEFAULT_EXCEL_PATH` (Bangalore) for a city with no file, so adding a city to `AVAILABLE_CITIES` never breaks planning. `MENU_EXCEL_PATH` still pins ONE workbook for every city (single-city deployments, tests) and then also switches off the per-city category declarations, because the file's contents no longer follow from the city name. **The caches are keyed by resolved path, not city name** (now `OntologyRepository`'s `_menu_data_by_path` / `_nonveg_by_path`, and `(path, pool tokens)` for `_filtered_by_path_and_pools` — see §4.5b; they used to be module globals on `api.app`): every city in `AVAILABLE_CITIES` has its own workbook today, so the sharing case is a city nobody has added a file for yet — it reads Bangalore's, and keying by city would hold two copies of a 6,000-row df — while keying the F5 pool cache by tokens alone (as it was) would hand a Pune client Bangalore's `common` pool. Everything derived from the ontology is city-scoped too: `repository.nonveg_items(city)` (Pune is all-veg, so nothing renders red) and `repository.item_names(city)` (a `constant_items` pin naming a dish only another city carries stays a stamped constant instead of becoming a candidate with no pool row). `/pool-preview` takes an optional `city`; `/editor-metadata` returns the union of tokens plus a `client_pools_by_city` breakdown, and `_validated_source_pools` validates against the city the client will have after the write.
15. **Client item pools (F5)**: the ontology `client` column tags each item with comma-separated pool tokens (exact-match, case-insensitive — never substring). A client's eligible universe = items in `common ∪ clients.source_pools`, deduped by `item_id`. `api._menu_data_for_client` filters the cached full df to that subset and rebuilds per-slot pools (cached per active-pool set) **before** `build_pools`, so the existing rule pipeline (theme/cooldown/ingredient_ban/…) runs on the merged pool — a borrowed item still obeys the target client's rules. `common` (always included) covers every mandatory slot, so filtering never empties one. When `source_pools` is `None` (pre-migration/column missing) the full ontology is used (unchanged behavior). **`FULL_POOL_CITIES`** (`src/constants.py`, checked first in `filtered_menu_data`) lists cities where the narrowing is switched off entirely and **every** client plans from the whole city list: **`bangalore`** (only 893 of 4,349 rows carry `common`; the other 3,456 sat in eight client pools while most clients had `source_pools=[]`) **`ncr`** (no `common` pool at all — all 1,630 rows tagged to one of eight sites: stryker 504 · carelon 443 · junglee games 353 · airtel noida 273 · sinch 247 · siemens 223 · sael 203 · corning 124, plus 93 untagged) and **`chennai`** (191 of 673 rows in eight per-site pools while three of the four new clients have `source_pools = []` — World Bank could not see the 21 rows tagged "World Bank", and TCL, which serves a veg biryani daily, could reach 4 of the 14 veg biryanis). NCR already reached the full list, but only via the empty-subset fallback below; naming it here makes that intentional and also keeps the whole list for a client who *does* set `source_pools`. **`hyderabad`** joined for the sharpest version of the same problem: its pool tags name the WRONG CITY. `hyderabad.xlsx` was seeded from Bangalore's list, so ~5,300 of its 6,260 rows carry a Bangalore client's token (healthineers/citrix/booking.com/…) — sites in another city that no Hyderabad client will ever select — leaving `common` at 960 rows and none of Quest's 101 additions, every one of which is tagged `quest`. It is a city-level switch on purpose — reversible in one line, no client row touched. **The check is on the RESOLVED workbook, not the city string** (`Path(city_excel_path(city)).stem`): a client whose `clients.city` is null reads Bangalore's list — that is what `city_excel_path` falls back to — but `normalize_city(None)` matches nothing, so it used to be denied Bangalore's *policy* and planned from `common` alone, roughly a fifth of the list, while reading the file the other four fifths live in. Stripe is one of five null-city clients (`scripts/backfill_client_city.sql`), and the effect was that every dish its own menu import had just added was invisible to it. Keying by resolved path is what the caches above already do (note 17): one identity, one policy. **No-`common`-city fallback** (the mechanism NCR used to rely on): a client whose eligible subset comes out **empty** falls back to the full city list rather than planning from nothing. And the city required-slot check is **not** applied to a filtered subset — it is ontology-integrity (enforced on the full list by `menu_data`); a slot a client's pool cannot fill surfaces per-counter in `diagnose`/the solve, not as a build-time 500. Per-client *rules* keyed to the pool identity are intentionally out of scope for now.
16. **Two-nonveg daily composition + theme-filter union**: the base ruleset's `slot_composition` rules (`nonveg_main_daily_pair`, `veg_dry_north_south_pair`) compose a counter's *pair* of nonveg mains / veg dries per day by theme. They self-gate on `requires_slot_count`, so single-slot counters are unaffected. Crucially, `ThemeSlotFilterRule` normally narrows `nonveg_main` to one cuisine on chinese/biryani/south/north days (chinese-gravy-only, biryani-only, …), which would delete the paired north/south chicken gravy before the solver sees it. So `_augment_nonveg_pair` (theme_rules.py) keeps `themed ∪ north/south-chicken-gravy` for `nonveg_main` **only when `cfg.slot_counts['nonveg_main'] >= 2`** (`_has_multi_nonveg`); single-nonveg counters keep the strict themed pool. It also keeps `_NONVEG_STRUCTURAL_FLAGS` (dry / tandoor / egg): the filter's job is to guarantee the *themed* dish is available, not to make every dish themed, and narrowing a 5-dish station to biryani + chicken gravy left it 0 dry and 2 egg items against a composition wanting one of each daily. The themed dish is still guaranteed — the composition rule mandates it. The composition rule then places one themed dish + one regional gravy. That retention used to start at **>= 3**, which left the same hole at 2: Bakertilly's "only on biryani day we will have chicken dry" needs a dry available on its biryani day, the narrowed pool held none, and the component silently dropped so Wednesday came back biryani + gravy. Widening at 2 cannot change what the **default** ruleset produces — with two cells and `nonveg_main_daily_pair` mandating the themed dish plus a regional gravy, both cells are already determined — it only lets a client that overrides that composition have its second dish be available.
19. **One staple declaration, four consumers.** `repeatable_item_flags()` → `MenuSolver._declared_repeatable()` → `context['extra_repeatable']` is read by `unique_items.apply()`, `item_cooldown.pre_filter_pool()`, `slot_composition._horizon_limited_components()` and (via `_peer_rules`, since diagnose() has no context) `unique_items.diagnose()` + `item_cooldown.diagnose()`. Each one that misses the declaration fails differently and quietly: the cooldown starves the slot a week later, the composition swaps a daily mandate for a floor of one ("chapati daily" → "chapati once"), and diagnose() reports a shortfall the solver will not hit. When adding a rule that reasons about "how many distinct items does this need", wire it to the declaration.

20. **A one-slot `slot_composition` component IS "this slot must be a matching dish".** `sum(matching vars on the day) >= count` over a family with one cell and `count: 1` leaves the cell no choice. That is the primitive for "chapati daily in indian bread" and "welcome drink will have buttermilk daily". `fixed_daily_item` is a *different* rule and the wrong tool for it: it only makes the matching dish CONSISTENT across days and leaves the solver free to serve a non-matching dish instead (Amadeus Pune got phulka). Use `fixed_daily_item` when something else already mandates the dish and you want it to be the same one; use a composition component when you want the dish.

18. **Staple-ness is regional, so it is declared by a rule, not a constant.** `REPEATABLE_ITEM_FLAGS_BY_SLOT` (constants) is global and correct for the chicken kebab; the new `repeatable_items` rule type puts the same declaration in a **city or client** ruleset. Pune's rulebook R36 states plain chapati/phulka may run on consecutive days — its bread pool was exactly those two dishes, so under the 20-day cooldown week 2 would have no bread at all — while a Bangalore bread slot rotating naans and parathas wants the no-repeat rule intact. **Keep the selector as narrow as the client's sentence**: an exemption is permission to repeat EVERY DAY, so it must not be phrased in terms that something else may legitimately widen. R36 and Chennai's twin selected on `is_plain_phulka_chapathi`, and `bread_form_flags.py` correctly widening that flag (Pune 2 → 9 rows) would have licensed a beetroot chapati daily — a no-repeat exemption growing as a side effect of a data correction. Both now name the two dishes. Both consumers read one declaration: `MenuSolver._declared_repeatable()` → `context['extra_repeatable']`, which `unique_items` folds into its repeatable set **and** (new) `item_cooldown.pre_filter_pool` exempts from history bans. Wiring only unique_items is the trap: the exemption then reads as applied while the cooldown still starves the slot a week later. `ItemCooldownMenuRule.diagnose()` reads the same declarations via `_peer_rules` so the pre-flight report and the solve agree — without that, a 2-item bread slot with both dishes in history is reported as a blocking ERROR the solver would not have hit.
13. **Colour variety is dynamic + diagnosable**: its numbers now come from the city ruleset (`color_variety` rule → `solver_overrides()` → `SolverConfig`); Bangalore ships none and keeps the defaults below. The built-in colour rule (`MenuSolver._add_color_constraints`) clamps `min_distinct` to `min(configured, colour-slots-configured, **colour-cells-this-day**, colours-present)`. The per-day cell count is the one that is easy to miss: `skip_cells` removes cells the config still counts, so a day serving 2 of its 5 colour slots was asked for 3 distinct colours (Amadeus Pune's Sunday). Colours-present cannot catch it — colours available and cells available are different numbers so small counters (e.g. a 2-slot Chinese station) aren't trivially INFEASIBLE. `diagnostics.color_variety_diagnostics` pre-flags the *provable* residual (`colours_available × max_same_color < colour_cells`) as an ERROR before the solver runs. **Asymmetric per-colour cap (rulebook 89-91)**: every colour ≤ `max_same_color_per_day` (=2, rule 90) EXCEPT up to `max_colors_at_reach` (=1) colour may reach `max_same_color_reach` (=3) — encoded as `sum(colour) <= soft_cap + (reach-soft_cap)*b` with a reach-bool `b` and `sum(b) <= max_colors_at_reach`. Rice≠gravy colour holds unless `ignore_rice_gravy_color_diff_on_chinese_day` (rule 92); colour-counted slots = `cfg.color_slots` (excludes white_rice/curd, rule 93).
14. **Solver time budget is a total, not per-attempt**: `MenuSolver.solve` enforces a wall-clock `deadline` across the ≤8 restart attempts (each capped at `min(slice, remaining)`), so `time_limit_sec` bounds total wall-clock (was `max(20, total/8)` × 8 ⇒ up to 160s for a 60s request).
11. **Optional/combo categories**: `curd` (plain-curd station, pool from `is_plain_curd`; in `REPEATABLE_SLOTS` so it's exempt from unique_items + cooldown and may recur daily), `curd_rice` (pool from `is_curd_rice` flag) and combos `dal_rasam`/`sambar_rasam`/`dal_sambar` (dal majority) are base slots in `DEFAULT_OFF_SLOTS` (selectable, off by default). **`curd_side` (Curd/Raita), `curd_rice`, `soup` and `healthy_rice` are in `COOLDOWN_EXEMPT_SLOTS`** — exempt from the 20-day item-cooldown ban but, unlike `REPEATABLE_SLOTS`, they KEEP `unique_items`, so they still serve distinct dishes within a week and only repeat once every distinct one is used (freshness rotates least-recently-served first — "after a whole cycle"). These condiment/side slots have small pools (2-13 distinct); a rolling multi-week sweep found the hard cooldown drained them empty by week 2-3 and turned the solve INFEASIBLE on ~18 counters. The cross-week no-repeat window is wrong for a side, so only the *ban* is dropped (pan-India); wired in `ItemCooldownMenuRule.pre_filter_pool`/`diagnose` alongside `REPEATABLE_SLOTS`, `test_curd_categories.py::TestCurdSideCurdRiceCooldownExempt` pins it. Complementary data fix: `scripts/expand_side_pools.py` adds 7 new veg dishes to each of `healthy_rice`/`dessert`/flavoured-chapati `bread`/`starter` in every city (Pune had 1 healthy rice, 0 starters), so the non-exempt of these (dessert/starter/bread) have enough distinct dishes to survive too. `curd` and `curd_side` (displayed "Curd / Raita") are the two yogurt-side options and are mutually exclusive per counter (`MUTUALLY_EXCLUSIVE_SLOT_GROUPS`, enforced in `ClientConfigLoader._validate_counters`); `curd_rice` is independent. Combos split one slot across the week by course_type (majority/minority via `combo_minority_count`, applied per-day in `MenuSolver._build_cells` via `_combo_day_variant`). **The two components ALTERNATE** — client rule: "when the combined slots are used it should alternate". The minority used to fill the LAST `combo_minority_count(n)` days, so a working week ran dal/dal/dal/sambar/sambar; `_combo_minority_days` now spreads it evenly, giving dal/sambar/dal/sambar/dal. The counts are unchanged and the split is still deterministic by day index (regenerate stability); because `combo_minority_count` keeps the minority at or under half the horizon, an even spread can always avoid two minority days in a row, which `test_menu_solver_helpers.py` pins as a property across all three combos and every horizon length 2-21. Optional slots are skipped by the mandatory-pool validation. A combo must be listed in `EXEMPT_FROM_CUISINE` or its minority component (often a different cuisine) gets theme-filtered out on off-theme days and the split silently collapses to all-majority.
6. **History split**: `menu_history` is one JSON document per client-day (`menu={slot:item}`), exploded to item-level in memory for cooldowns; `week_signatures` is a weekly hash for week-level cooldowns.
7. **Supabase is the source of truth** for clients, history, overrides — Flask and Streamlit both read it directly.
8. **Slot expansion**: base slot names like `veg_dry` get expanded to indexed slots `veg_dry__1`, `veg_dry__2` in `PoolBuilder._expand_slots_in_order`. Rules operate on expanded names.
9b. **Per-counter rule scoping**: a `client_rules.json` block may carry a `counters` map — `{"counters": {"<counter name>": {disable, rules, constant_items}}}` — layered over the client-level entry. A rule that only applies to one station (L&T's `Non Veg Lunch` is themed biryani daily, so the weekly nonveg-biryani cap is dropped there) stays scoped instead of silently switching off for the client's other counters. `load_for_client(client, generic, counter_name)` and `get_client_constant_items(client, counter_name)` take the counter.
9c. **Rules are never relaxed just to return a menu**: an over-constrained counter fails with a message naming the tightest slots, because the useful output is the conflict, not a plan that abandoned the rules. The single exception is *provable arithmetic impossibility* — a slot with fewer distinct eligible items than days to fill (`unique_items_menu_rule.starved_slots`) — where uniqueness is lifted for that one slot only, a HIGH-tier repeat penalty still maximises variety, and `diagnose()` reports it. Frequency targets cap to `min(placeable_days, distinct_matching_items)`: counting placeable days alone asked for two liquid desserts from a pool holding one. Every config-driven rule type (`selector_frequency`, `slot_composition`, `attribute_grouping`, `unique_items`, `curd_side`) implements `diagnose()`, so an inert or under-enforced rule is reported rather than silently dropped. Worked example of why the exception is arithmetic and not a fallback: `common` holds exactly 4 distinct `curd_rice` items, so a common-only client running that station over 5 days must repeat one. The other 9 `curd_rice` items in the ontology are client-specific health variants (millet / brown-rice / dry-fruit) belonging to `healthineers`/`continental`/`cloudera` — attaching one of those pools to fix the count would import a whole unrelated client's menu, so the right resolution is a 5th `common` item, which is what the diagnostic asks for.

9e. **A frequency cap can be *forced* past, and that must be detected, not discovered as INFEASIBLE**: `max`/`max_per_week` was assumed "only tightens, never wrong". It is wrong whenever the counter cannot avoid the selector on more days than the cap allows, and there are two ways that happens — the theme filter leaves the slot nothing else (`SelectorFrequencyRule._forced_days`), or a `slot_composition` mandates a matching item (`slot_composition_rule.days_forced_by_composition`, cross-checked via `_peer_rules`). Both are provable from the pools, so they are ERRORs and `/plan` answers 422 naming the rule, the day count and three concrete fixes. Worked examples: Amadeus's Chinese counter is themed `chinese_continental` every weekday, so on an odd ISO week all five days resolve to *continental* and the theme filter narrows `rice` to continental rice — five forced days against `continental_rice_weekly`'s `max: 1`, INFEASIBLE on 9 of 14 start dates while `/diagnose` said `would_succeed: true`. Siemens Technology's non-veg counter is the composition variant: two biryani-theme days each get a mandatory biryani against `nonveg_biryani_once_per_week`. Note `max` counts **days**, not dishes — a counter with 2+ nonveg slots could satisfy the weekly cap by stacking two biryanis onto one day, which is what `nonveg_biryani_one_per_day` (`daily_max: 1`) exists to stop.

9g. **Some dishes are staples, not variety slots — model that instead of relaxing the rule that wants them daily**: L&T's 5-dish non-veg station needs a kebab every day, and only one kebab is eligible for a common-only client. Read as an ordinary dish that is a five-distinct-items shortfall; read correctly, the chicken kebab is a **staple** like steamed rice — the same dish daily — so it is exempt from `unique_items` and the cooldown ban and one item covers the week. That is what `REPEATABLE_ITEM_FLAGS_BY_SLOT` encodes. Separately, `_horizon_limited_components()` + `_add_horizon_floors()` still handle a *genuine* shortfall in a non-staple component (distinct items < days it is required, which per-day availability never reveals) by replacing the per-day mandate with an at-least-N-days floor via `OnlyEnforceIf` indicators — same bug class as the `selector_frequency` distinct-items cap (note 9c), one rule type later. A staple component is skipped by that check entirely.

9f. **Sweep more than one start date**: `chinese_continental` resolves per ISO-week parity, so satisfiability is date-dependent and a mid-week start spans two ISO weeks (the theme flips inside a single horizon). The all-clients sweep originally used one Monday of an even week and passed Amadeus's Chinese counter while it was failing on most real start dates. `test_every_counter_generates_on_other_start_dates` covers an odd-week Monday and a Wednesday, and accepts 200 **or** a 422 that names a rule and a fix — an unexplained 500 is the failure it exists to catch.

21. **The load path is already city-scoped and lazy — measured, not assumed.** Caches start empty; one Chennai `/plan` loads exactly ONE dataset (`chennai.xlsx`, 361 rows, 22 MB peak heap) and never touches Bangalore's 4,321-row master. `/clients`, `/client-config/<name>` and `/health` load **zero** datasets (1-2 ms). `/diagnose` and `/pool-preview?city=` load one (0.4-0.6 s cold). So "city → that city's dataset → that client's rules" is what the code does; there is no eager whole-ontology load to remove.

23. **Cadences longer than the plan horizon are enforced from saved history, not within one solve.** A 5-day plan cannot see prior weeks, so "once per 15 days / fortnightly / monthly" rules can only be enforced by reading `menu_history`. `selector_history_window` (note in §4.2) is the mechanism: `api.app` resolves each such rule's selector to concrete item names against the city's ontology (`matching_items(df)`), passes `(items, window_days)` pairs to `_build_history_context`, and `HistoryManager.selector_banned_by_date` bans the whole family on any planned date within `window_days` of a saved occurrence — merged into the SAME `banned_by_date` the item-cooldown pre-filter already applies (so no solver change). It pairs with a within-plan `selector_frequency max`/`daily_max`: the window handles ACROSS plans, the cap handles WITHIN one. `_effective_history_window` now also scans `window_days`, so a 30-day monthly cadence widens the history query. **`matching_items` is scoped to `base_slot`** (by `course_type`) when one is set: "leafy veg_dry once per 15 days" must not fire because a leafy *dal* was served last week — matching by flag alone banned the whole family across every slot and starved Pune's dal the week after a leafy dal was saved. A fresh plan (no history) is never affected — the ban only triggers on a real prior occurrence, so this can't make a first-week plan INFEASIBLE. This is the selector-level twin of the per-dish `item_cooldown` (which is why they share the ban map). Applied across every city (Bangalore/Pune) and the NCR clients that stated such cadences. Week-level co-occurrence ("biryani not the same week as fish") is now handled separately by `same_day_exclusion` with `"scope": "week"` (Stryker NCR), a within-plan constraint rather than a history read; the positive "serve it once per N days" direction stays deferred.

24. **Menu variety is a soft freshness objective, not a longer cooldown.** The hard item cooldown (default 20 days) only *bans* a dish for a window; the moment it lapses nothing stops the solver reprinting day-1's menu on day 21. The fix is a preference, not a longer ban: `HistoryManager.days_since_last_served(as_of, const_slots, repeatable_items)` builds `{item_base: days-since-last-served}` from saved history (staples that recur by design are excluded — steamed rice, the daily kebab), `_build_history_context` returns it as its 4th value, and it is threaded through `SolverInputs.recency_by_item` into `MenuSolver`/`MenuRegenerator`. `MenuSolver._freshness_bonus(item)` = `min(days, FRESHNESS_CAP_DAYS=30) * FRESHNESS_UNIT=3000` (a dish absent from the map — never served in the window — scores the full cap), added per candidate in `_build_objective`. The weights are deliberate: `CAP*UNIT` (90k) sits **below** one LOW soft-rule unit (1e6, `OBJECTIVE_TIER_WEIGHTS`) so any real rule still outranks freshness in a cell ("unless a rule is forcing it"), while `UNIT` (3000) sits **above** the random tie-break (max ~1000) so freshness actually orders candidates; ties among equally-fresh dishes fall back to the tie-break, preserving cross-solve variety. It never bans, so it can never make a solve INFEASIBLE, and an empty recency map (regenerate paths / tests) yields 0 bonus — historic behaviour. Pairs with the existing within-plan `unique_items` (no repeats inside one horizon) and `week_signature_cooldown` (avoid recent whole-week signatures): together they spread item usage as far as the pools allow. `test_freshness_variety.py` pins the recency map, the bonus bounds, that the least-recently-served candidate wins an unconstrained cell, and that an all-recently-served map never forces INFEASIBLE. **`REGEN_SIMILARITY_PENALTY` is derived from the cap, not written as a literal**, because the two pull on the SAME dish: `MenuRegenerator` first tries to forbid the dish being replaced and falls back to a penalty when the hard block would empty the cell, and the dish it is penalising is usually the one freshness likes most — a dish absent from the recency map scores the full cap, and absent is the common case since the map holds only what has been saved. At the historic `-10_000` against a `+90_000` cap the arithmetic was not close: the solver re-picked the dish the user had just asked to replace and the button appeared to do nothing. Now `-3 * MAX_FRESHNESS_BONUS` (-270k) — a clear margin over freshness, still an order of magnitude under one LOW unit so the rule tiers are untouched and a soft preference can still keep a dish in place. `TestRegenerateOutbidsFreshness` pins it as an *ordering between the two constants*, so tuning either cannot quietly re-open it.

22. **Cross-counter common categories are a client-orchestrated pin-from-primary pass, not a joint solve.** A client declares `shared_categories` (a list of base slots) — from the **editor** (`clients.shared_categories`, a nullable JSONB column; the multi-counter editor shows a toggle + a multiselect of slots present on 2+ counters) **or** from `client_rules.json` (`MenuRuleLoader.get_shared_categories()`, e.g. DXC). `GET /client-config` returns the DB value when set and **falls back to the file value** otherwise, so both sources feed the same planner path. Config layer mirrors `source_pools`: `get/set_client_shared_categories`, `_normalize_shared_categories_value` (keeps known base/const slots only), threaded through `get_client_row`/`create_client`/`update_client_atomic`/`_apply_fields_individually`. The planner (`app.py` multi-counter loop) solves the **primary** counter (index 0) first, extracts its dish for each shared base slot per day via `ui.formatters.shared_items_from_solution` → `[[iso_date, slot_id, item], …]`, and passes them as `shared_items` to every later counter's `/plan`. Server-side, `api.app._merge_shared_items` folds them into `forced_items` — the SAME narrow-the-cell mechanism a `constant_items` pin uses (note 9), so every other rule still sees the pinned dish. An explicit client constant already in `forced_items` **wins** (a sibling counter never overrides the client's own config); a shared item that is not eligible for the target cell is logged and the cell solves normally (never INFEASIBLE from the sync). Non-shared slots (e.g. a non-veg station's `nonveg_main`) still solve independently. DXC uses it for bread/rice/sambar/rasam/curd_side/dessert/white_rice. **A counter may opt OUT**: `shared_categories_excluded_counters` in the client's rules file (`MenuRuleLoader.get_shared_category_exclusions`, surfaced by `GET /client-config`, read by the planner, which simply sends that counter no `shared_items`). File-only, no DB column and no editor control, because it is a statement about the menu's structure like the rules beside it. ICON Chn needs it — "rice combo counter is not linked to the shared items list it has seprate menu" — and `clients.shared_categories` is otherwise all-or-nothing per client. Remaining limits: (a) it makes the PRIMARY the source of truth rather than optimising all counters together (docs/client_logics.md gap 1); (b) the source is always the primary, so "the same non-veg on counters 2 and 4" — ICON Chn again — cannot be expressed at all. Naming a source counter per shared slot is the change that would cover both.
    **`/editor-metadata` used to be the one exception** — it loaded EVERY city (3 workbooks, 4,956 rows, 4.8 s cold) because `_city_pool_tokens()` loops `AVAILABLE_CITIES`. Now **12 ms cold, zero datasets**: the answer is a handful of strings per city, so `scripts/build_pool_token_map.py` commits it to `data/raw/city_items/pool_tokens.json` and the endpoint reads that, falling back to the workbooks when the file is absent (a fresh checkout is slow, never wrong). `_city_pool_tokens(only_city=...)` also scopes the work when a city IS known, and `ui.api_client.get_editor_metadata(city=...)` can pass one. Staleness is the price of a derived artefact; `test_pool_token_map.py` recomputes from the workbooks and fails on disagreement, and also asserts the endpoint answers identically with and without the map. Re-run the builder after any re-import — the source_workbooks README lists it. Historic detail (3 workbooks, 4,956 rows, 4.8 s cold / 23 ms warm) because `_city_pool_tokens()` loops `AVAILABLE_CITIES`. Passing `?city=` does *not* avoid it — the param filters the result after all three loads. It is a **one-time per-process cost**, not per-request. It cannot simply be scoped either: `customisation/main.py` fetches metadata ONCE at line ~109, before `selected_city` exists (~line 207), then switches cities client-side out of `client_pools_by_city`, so the full map is genuinely needed by the current UI. Fixing it means either precomputing the token map into a small committed JSON (fast, but a new artefact that can go stale — would need a test comparing it against the workbooks, like the correction scripts) or splitting the endpoint so pool tokens are fetched per city after one is chosen (API + UI change). Reading only the `client` column is NOT a fix: openpyxl parses the whole sheet regardless, measured at 1.1x.
    **No unused API surface**: 13 of 14 endpoints have a UI caller; `/api/v1/metrics` has none by design (Prometheus scrape).

9d. **Diagnostics are aggregated per slot, not per (day, slot)**: `theme_slot_filter`'s narrowing INFO fired once per themed day per slot — 811 entries across the 56 counters, each captioned "No action needed" — which buried the 10 real WARNINGs. It now emits one line per slot carrying the day count and the resulting pool range (283 entries, same information). When adding a `diagnose()`, aggregate before appending: the list is read by a human deciding whether to fix data.
9. **Per-client custom rules**: `data/configs/clients/<slug>.json` — one file per client — stores per-client overrides keyed by client name (the single 2,453-line `client_rules.json` was split; the loader still falls back to it when the directory is absent). **The key must equal `clients.name` byte-for-byte** — the lookup is exact-match with no normalisation, and a mismatch is silent: every rule for that client loads as zero, `/diagnose` still reports clean, and a plausible-looking plan comes back having ignored all of them. `ToastTab CHN` was configured in the DB while this file said `Toast Tab CHN`, and the only symptom was white rice on a north day. Shape is `{disable: [city_rule_names], rules: [...], constant_items: {slot: value|{weekday: value}|[a, b]}}` (legacy bare list still works as `{rules: list}`). A `constant_items` value that is a **list alternates across ISO weeks** (even week → first, odd → second, index = `iso_week % len`, the same parity `chinese_continental` uses), usable bare (alternate every day) or as a weekday-map value (`{"wed": ["Hyd Mutton Biryani", "Fish Tikka Masala"]}` → Wednesday only, mutton one week / fish the next — Siemens Technology's non-veg counter). Resolved in `_resolve_client_constant(spec, weekday, iso_week)`, threaded from both the forced-item build (`api.app`) and the post-solve stamp (`menu_solver`) so the two agree. `load_for_client()` merges by rule `name` via the same `_merge_rule_dicts` used for city `extends` — same name overrides, `disable` drops. `constant_items` are honoured **two ways, chosen by whether the dish exists in the ontology**: a pin naming a real item becomes `SolverConfig.forced_items[(date, slot_id)]` and its cell stays in the model with candidates narrowed to that dish, so every other rule sees it (its colour counts toward colour variety, its cuisine toward cuisine variety, and unique_items stops a duplicate elsewhere); a pin naming a dish the ontology does not carry ("Mutton Biryani", "Fish Tikka Masala" — the ontology holds only chicken + egg for non-veg) has nothing to narrow to, so its cell is skipped and the text is stamped verbatim post-solve. Adding the dish to `menu_items.xlsx` switches the same pin over to the solver with no config change. Mutually-exclusive sibling slots are removed either way. **A pin may also be a FIXED ITEM the counter serves without a station** — a daily curd, papad or welcome drink is served every day and never chosen, so requiring a rotating category for it was asking the operator to configure a station that does not exist. `_resolve_constant_items` drops a pin for an unserved slot only when it is AMBIGUOUS, i.e. the client has other counters the pin might have belonged to; it is kept when the client has one counter (`ClientConfig.counter_count`) or the pin sits in that counter's own block (`MenuRuleLoader.get_counter_scoped_constant_keys`). The second half is in `_rules_and_skip_for_client`: only a slot the counter actually SOLVES may be forced. `pools` holds every slot the ONTOLOGY can fill, so a kept pin used to match a real dish, land in `forced_items`, and then be dropped by BOTH paths — no cell to narrow, and the post-solve stamp skips anything in `forced_items` as already placed. Booking.com's daily curd vanished exactly this way, as did Telstra's and Tessolve's curd, H&M's welcome drink and Quince's and Sinch's raita. `api._canonical_item_name` does the space/underscore matching. Client `working_days` (DB column) filters the plan horizon (e.g. Quince = Wed/Thu/Fri).

26. **"Weekly" has to mean weekly on a plan longer than a week.** `selector_frequency`'s `max` counts days across the WHOLE HORIZON. That is right for a one-week plan and silently *tightens* as the plan grows — `max: 1` means "once a fortnight" on 14 days and "once in three weeks" on 21 — and sixty shipped rules were named `*_weekly` / `*_once_per_week` while behaving that way. `max_per_week` is the addition: the same count bucketed by **ISO calendar week** (Monday-Sunday, the boundary `chinese_continental` already resolves its parity on — a calendar week, not a rolling 7-day window, so a Thursday and the following Monday are legitimately different weeks). `max` keeps its meaning, so a rule that really is "once per plan" can still say so. `nonveg_biryani_weekly` was the sharpest case because its key was **already called `max_per_week`** while summing over the horizon — the name promised the semantics and the code did not deliver them — and it is the most easily contradicted: a biryani-theme day narrows a single-nonveg counter to biryani only, or `nonveg_main_daily_pair` mandates one, so the second biryani day is FORCED rather than chosen and the cap then reads as impossible. **Two halves have to agree** (`tests/rules/test_per_week_caps.py` reads the source to check both): `apply()` constrains per ISO week, and `diagnose()` buckets the forced days by week and compares the busiest one. Comparing a horizon total against a weekly number is what made a satisfiable rule report as impossible; comparing per-week while constraining per-horizon would hide a real conflict. That is also why `slot_composition` gained `dates_forced_by_composition()` — the old `days_forced_by_composition()` returned a COUNT, and a count cannot be bucketed by week, so the per-week check had nothing to work with and fell back to the horizon total. The effect was fleet-wide: at 7 days 74 of 85 counters diagnosed clean, at 14 days only 34, and almost every block named one of these rules; with per-week caps it is 85 of 85. Sixty configs were migrated `max` → `max_per_week` and a test fails if a `*weekly*`-named `selector_frequency` rule goes back to `max`.

29. **A day the client does not work is a BLANK column, not a missing one.** The horizon spans `num_days` **weekdays** (calendar days for a `serve_weekends` site), and the client then serves some subset: `_weekdays_from` counts up to `num_days` skipping Sat/Sun, then `_filter_dates_by_working_days` drops the weekdays a restricted-`working_days` client does not cook on (Clario Mon-Thu, Piramel Mon/Tue/Thu, Quince Wed/Thu/Fri — 3 of 61 clients). Those dropped dates used to be gone for good, so a 5-day horizon from Monday returned FOUR days for Clario: no Friday column at all, the gap closed up, and "5 days" quietly meant a different number of columns per client. Now `_prepare_solver_inputs` keeps both — `weekday_dates` is what the solver plans, `SolverInputs.span_dates` is what the menu is rendered over (`_span_dates` unions them, so a date the solver dropped is still shown). No solver change was needed: `SolutionFormatter` reads `week_plan.get(d, {})`, so a date with no plan formats as a day with no items, and no rule sees an extra day (`selector_frequency.apply` does `if not day_cells: continue`, so a blank Friday adds nothing to a `max_per_week` bucket). The blank day is **marked**, not just empty — `served_dates` gives it `is_working_day: False` and no theme, because a cuisine tag over an empty column reads as a day the solver FAILED on rather than one the kitchen is closed for; `ui.formatters.off_days_from_solution` reads the flag and `menu_table_html` renders a dimmed "Not served" header (`.day-off`). The Excel export needed nothing — it already builds cells with `plan.get(d, {}).get(slot_id, "")`. The planner's horizon **default** follows the client too (`_cached_week_length`): 7 for a weekend site, 5 otherwise, reset when the client changes because a keyed Streamlit widget would otherwise carry a 5 over to a weekend site. `tests/platform/test_non_working_days_are_blank.py`.

28. **`menu_history` is read back, so what `/save` writes has to be the ontology's own spelling.** The cooldown (`banned_items_by_date`), the freshness objective (`days_since_last_served`), the cross-week cadences (`selector_banned_by_date`) and the week signatures all compare the STORED STRING against the ontology's `item` column — `ItemCooldownMenuRule.pre_filter_pool` is literally `pool['item'].isin(banned)`. `/save` used to store whatever it was handed. A plan from this client's own `/plan` round-trips fine (`item_base` IS the ontology spelling), which is why nothing ever showed on the normal path; anything else failed **silently and in the worst direction** — a display name "Dal Tadka" stored as `dal tadka` matches no pool row, so the dish is never banned and never ages, with no log line, no diagnostic and a plausible menu every week. `api.app._canonicalise_saved_plan` now resolves each dish through the same space/underscore matching a `constant_items` pin uses (`_canonical_item_name`) and stores the ontology's spelling, turning the silent failure into a correct save rather than a warning about an incorrect one. It runs **before** the week signature, which is compared against future weeks and so must be computed over the stored spellings. What does not resolve is stored as given and returned in `unknown_items` (+ a `warning`; both keys absent on a clean save, so the body is unchanged when there is nothing to say) and logged — **never rejected**, for two reasons: an off-ontology pin is a real feature (note 9, five clients rely on it, so `_declared_constant_values` excludes them from the report), and a plan the user has just spent a solve generating must not be lost to a name we failed to recognise. A wrong cooldown is recoverable; a discarded plan is not. Same reasoning for the failure path: if the item list cannot be read at all, the save proceeds unvalidated. `tests/platform/test_save_canonicalises.py`.

27. **A rule instance may not be shared between two solves.** `OntologyRepository.rules_for_city` caches one ruleset per city for the process, and rules are not stateless: `UniqueItemsMenuRule.apply()` stores the model's repeat-penalty **variables** on `self` for `get_objective_terms()` to read back. `load_for_client` used to return that cached list unchanged for a client with no overrides — most of the fleet — so any two clients of one city solving concurrently (which `@solver_gate` allows by design: 1 active → 9 workers, 2 → 5 each) shared every rule object, and the second `apply()` clobbered the first's list. A CP-SAT expression carries variable INDICES, not objects, so the foreign variables are not detected as foreign; they are resolved against whichever model is solved. Measured (`tests/rules/test_rule_instance_isolation.py`): if the other model is **bigger** an index runs past this model's variable list and CP-SAT answers MODEL_INVALID → a 500; if it is the same size or **smaller** every index resolves to the WRONG variables, the solve returns OPTIMAL and the menu is served having optimised something unrelated, with nothing logged. The quiet case is the common one, so "it 500s under concurrency" understates it. Fixed in the loader (`MenuRuleLoader._rebuild`): `load_for_client` now always returns fresh instances, rebuilt from each rule's `config`. Costs one `_create_rule` per rule against a CP-SAT solve, and the override path always did it — the early return was the only sharing route. A rule that cannot be rebuilt is passed through rather than dropped, since losing a constraint silently is worse than the hazard. When adding per-solve state to a rule, this is the invariant that makes it safe. `UniqueItemsMenuRule._repeat_penalty_vars` is the sharp case but not the only writer: `diagnostics.run_diagnostics` assigns `rule._peer_rules = rules` on every rule that declares the hook (seven types do), and `SelectorFrequencyRule._forced_days` / `NonvegBiryaniWeeklyRule.forced_biryani_days` record `_forced_dates` for the per-week check in note 26 — all per-request writes onto what used to be a per-process object.

---

## 10. Dependencies

Solver `ortools` · data `pandas numpy openpyxl` · web `flask flask-cors requests` · db `supabase python-dotenv` · UI `streamlit` · tests `pytest pytest-cov`.

---

## 11. Editing rules for this file

- When adding a new rule file under `src/menu_rules/`, append a row to §4.2.
- When adding an endpoint in `api/app.py`, append a row to §3.
- When moving or renaming a module, update its row in §4 and any mention in §7.
- Keep rows to one line. This file is optimized for fast scanning, not prose.
